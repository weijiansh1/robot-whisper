# 去噪截断 CPU 先导实验 — 预注册（2026-08-22，启动前冻结）

## 动机与门（已完成的离线证据）

`audit_flow_stop_gate.py`（flow-lead-cpu-t0s24，44 query × 16 候选）：
一跳外推 x̂ = x_t − t·v_t 在 r=5–6 轮后与完整 10 轮终点的相对 L2 中位 0.048–0.042，
已低于"换一个噪声 seed"的候选间差异（0.049，已知不动成功率）并落入随机换专家的
无害带（0.04–0.06）。裸 x_m 不可用（r=9 仍差 14.5%）。路由读出在所有 m 上劣于 x̂。
该门来自 goal/t00 单任务几何——本先导测它对 t08 成败的外推。

## 假设

H0：r=6 截断（+一跳外推）不改变 t08 成功率。
H1：截断降低成功率。

## 设计（冻结）

- 任务：libero_10 / t08（唯一有结局方差的捕获任务，成功率 ~57.8%）。
- 设备：CPU（两张 H20 被其他租户占满；CPU 推理 bit 确定已证）。CPU 轨迹与 GPU
  捕获不可逐集配对，故自带对照臂。
- 两臂，串行，配对设计：`arm-r10`（对照，r=10 数学恒等于原始采样器——补丁在
  最后一轮缩放因子恰为 1）与 `arm-r6`。同 16 scene {0,3,…,49} × 8 draw
  （seed 1000–1007）= 128 集/臂，逐 (scene, seed) 配对。
- 服务配置两臂完全一致（同补丁、同 --no-route-capture、同线程数），仅 r 不同。
- 冒烟：每臂先跑 scene0 × 2 draw，失败即中止，冒烟集不进分析。

## 端点与判据（冻结）

- 主端点：配对成功差 Δ = P(r6) − P(r10)，精确 McNemar 双侧 p + 不一致对计数，
  外加逐 scene 聚类 bootstrap 95% CI（10k 次，scene 为重抽单元）。
- 次端点：步数差、每次推理均值耗时（验证省时比例）、b/c 翻转方向表。
- 判定：
  - Δ 显著为负且 |Δ| ≥ 8pp → 截断有害，方向关闭；
  - CI 完全落在 ±6pp 内 → "无灾难"，升级 GPU 512 集确认（等卡空）；
  - 其余 → 功效不足，仅报告，不下推广结论。
- 不中途窥视；分析在两臂全部完成后一次进行。n=128 只能排除大效应——这是先导，
  不是确认。

## 产物

`runs-trunc-pilot/{smoke-r10,smoke-r6,arm-r10,arm-r6}/`，日志在 `runs-trunc-pilot/logs/`。
实现：`serve_with_recorder.py --trunc-rounds/--no-route-capture/--gpu cpu`（本日新增），
`run_corpus_capture.py --trunc-rounds` 透传。
