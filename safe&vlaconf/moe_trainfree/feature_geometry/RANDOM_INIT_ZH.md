# 初始化敏感性补充

首次投影后发现：10 维动态表征在 `init="pca"` 下，改变 random_state 得到了
完全相同的坐标。这些重复不构成三个独立的布局稳定性证据。

保留首次全部结果；对四套件、两种配对表征，在相同点和相同归一化上补做
`init="random"`、seed=20260907/20260908 的 t-SNE。其他参数不变。
不根据图形选择种子，不改变主图。图中明确区分 PCA 初始化与随机初始化。
另检查同一动态图在任意旋转/翻转下的外观，而不赋予左右上下固定语义。
