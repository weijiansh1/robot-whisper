# HiMoE-VLA MoE 路由研究结果汇编（2026-08-22）

对象：HiMoE-VLA 官方释出 checkpoint（Goal / Spatial / Object / Long / CALVIN-D 五个微调 + 共享架构：
AS-MoE@层 0,1,16,17（1-of-3，输入 data_mask）、HB-MoE@层 2–5,12–15（4-of-32 + shared expert）、
dense@6–11；每控制步 10 轮 flow 去噪 × 11 个后缀 token（1 state + 10 action）。
本文汇总 2026-08 全月与路由相关的全部结果，每条含**思想 / 做法 / 结果 / 分析**与复制状态。
统计口径与 scope 修正以 `himoe-vla-research-audit-2026-08-22.md` 为准。

**一句话总账**：推理期的 MoE 路由是各 token 输入的向量量化读出——state token 量化本体感受
（pose code，坐标系跟随动作空间），action token 量化当前噪声动作 latent；它对轨迹实现有完全
因果权威、对成功率期望中性；一切把它当决策信号的应用（结局预测、候选挑选、剪枝、停止器）
在匹配对照下全部关闭；专家侧则相反地丰富：稳定正交函数基 + 幅值增益控制 + 末段醒来。

---

## 第一部分：路由的结构性质

### 1. AS-MoE 是常数路由（构造性事实）

- **思想**：层级 MoE 的顶层（AS）宣称按动作空间分工——那么它在单一动作空间内应当恒定。
- **做法**：读代码（`ASMoE.forward` 的输入是配置常量 `data_mask`，非 hidden state）；在 312 / 1682 /
  30 任务 × 全部控制步上逐位核对实际路由；对 v1 ICLR 补充代码与 release 交叉比对。
- **结果**：LIBERO 全部 30 任务只有 1 个 AS 签名 [2,0,0,1]，CALVIN 为 [0,1,2,0]（不同 checkpoint）；
  层 1 是精确 bf16 三方平票（0.33325/0.33325/0.33334），选择由舍入决定；两代代码一致。
- **分析**：**设计如此，不是损坏**——AS 按 data_mask 分流，单一空间内当然恒定。层级顶层在部署期
  零方差；论文 v1 正文对门控机制的描述与代码相反（v2 注释掉该段）。复制：全部数据。

### 2. HB 动作 token 路由近均匀 = 设计目标达成；state token 反而锐利

- **思想**：区分"负载均匀（设计要的）"与"逐输入无差别（设计没要的）"。
- **做法**：27 万+ 路由站点的熵/top-1/top-4 质量统计，按 token 拆分；gate 权重与 kaiming 重初始化
  在真实 h 上做 gauge-correct 对比（softmax 平移不变，用中心化 W̃ 与 Tr(W̃·Cov(h)·W̃ᵀ)）；
  对 HB-Reg 损失做解析分解。
- **结果**：动作 token 熵/ln32 = 0.984–0.998、top-4 质量 1.1× 均匀、Pr[M₄>0.2]=0.0000%；
  state token 前块熵 0.80–0.89、top-1 至 0.956、Pr[M₄>0.2]=57.7%。训练把动作 token 的持久偏置
  对比压低 28–228×（输入驱动部分只压 8–11×），state token 的对比反而放大 2–3×。
  解析：U=11 时 HB-Reg = 1 + (N²/KU²)·Cov(c,S)——只读协方差不读方差，1 是参考值不是下界
  （可达状态到 N/(KU)=0.727）。
- **分析**：论文 :795 自述损失收敛到下界附近（≈均匀利用）——测量**确认设计**；新内容是
  逐输入层面的三件事：动作 token 通道被训练推向无差别、state token 是唯一被锐化的通道、
  以及小 U 下均衡损失的协方差盲。复制：3 checkpoint。

### 3. bf16 平票规制：离散专家身份的数值地基是松的

- **思想**：top-4 边界处的分数差有多大？若在数值精度量级，离散专家 ID 不可作信号。
- **做法**：镜像 gate 的 forward 拿到 topk 消费的原始张量（验证与原 gate 逐位一致）；与关闭
  autocast 的真 fp32 影子对比（教训：autocast 区域内的"fp32"仍被截获——第一次得到 0.00% 差异
  即为 bug 自报）；真实捕获数据上复核平票率；功能性检验=把选中的最弱专家换成第 5 名/随机。
- **结果**：32 个分数平均只有 23.9 个不同值；第 4/5 名精确平票 23.8%（真实数据 10,030/10,030
  复核）；模型 topk vs 确定性排序差 13.4%；**换成随机专家动作只动 4.08%，第 5 名 3.03%**
  （nearest/random=0.744）。
- **分析**：约 1/4 站点"谁入选"由舍入决定，且入选边界下的排序几乎不承载功能——离散 top-4 ID
  作为逐站点信号死亡；但注意这与"专家可互换"不同（见 §16）。幅值出自 n=1 合成观测 CPU，
  平票结构已在真实数据复核，幅值待 GPU 复核。

### 4. 后缀 token 的角色分工：一把尺子加一个开关

- **思想**：11 个后缀 token 的路由是否同质？若不同质，任何"后缀平均"都会抹掉信号。
- **做法**：逐 token 的路由统计与跨 rollout 对比；state token 的去噪轴/噪声轴方差直接测量。
- **结果**：10 个动作 token 携带固定位置码（相关长度 ~8 步）；1 个 state token 携带全部输入依赖；
  state token 路由跨 10 轮去噪 bit 级恒定（max diff = 0.0，100% 控制步）、步 0 跨 32 个噪声
  draw bit 级一致——**噪声驱动的 top-4 抖动全部在动作 token**。
- **分析**："永远不要在后缀上取平均"成为全项目铁律；存储上 state/AS 的 11 份去噪拷贝有 10 份
  冗余。复制：5 任务 3 checkpoint。

---

## 第二部分：路由的内容（它编码什么）

### 5. state token 路由是 pose code（核心正结果）

- **思想**：路由若不编码任务/结局，那它到底编码什么？正面鉴定 + 封口（"没有别的了"）。
- **做法**：正向——对 8 层×32 专家逐个做"proprio(8) → 该专家是否在 top-4"的 LOSO AUC，
  场景内配对打乱做零对照；反向——路由 → 姿态的 LOSO ridge，换算物理单位；预算——
  routing = f(proprio)+g(objects)+残差，逐步升级模型类（线性→二次→RBF→GBT）。
- **结果**：正向 AUC 0.92–0.999，出现单变量阈值细胞（e29@L2 对轴角 2：0.996）；反向 EEF
  26–57 mm、夹爪开度 4.8 mm RMSE（R² 0.966）；**离散 top-4 解码优于连续概率（0.892 vs
  0.831）**；非线性预算：proprio 单独解释 96.3%（GBT），加物体位姿零增益，3.7% 残差 =
  bf16 量化 + 平票抖动的量级。
- **分析**：路由 = 手臂状态的离散重编码；"离散优于连续"是码本（cell code）性质的直接证据。
  这解释了全部结局类 null：**路由不超越物理态，因为它就是物理态的压缩拷贝**。
  复制：t08 96%、跨 5 任务 86–95%、3 checkpoint。

### 6. 坐标系跟随动作空间（最强可推广正结果）

- **思想**：LIBERO 的 8 维状态无法区分"编码手臂"还是"编码末端"——CALVIN 的 15 维 robot_obs
  同时含工具位姿与 7 关节角，且该 checkpoint 在关节空间训练，问题在此可分。
- **做法**：同一录制器接入 CALVIN 服务；leave-one-instruction-out（33 条指令为组）；
  共线性对照：线性上 tool→joints 易（0.849）而 joints→tool 难（0.365），故"编码工具"的路由
  最多到 ~0.58。
- **结果**：路由 → 关节角 R² **0.831** > 工具位姿 0.584，突破共线性上限 ⇒ 直接编码关节；
  LIBERO（EEF 空间训练）反之编码末端。纯度随输入丰富度下降：LIBERO 95.8% vs CALVIN 72.4%
  （33 条指令自身占 15.5%）。
- **分析**：**pose code 用动作空间自己的坐标系书写**——量化器忠实于其输入分布的支配变量。
  出处注意：该 run 是截断探针（80/1000 序列、episode_id 全零），分割方法本身干净，
  发表前需干净重采（已排队）。

### 7. 夹爪门：唯一的输入驱动离散开关

- **思想**：近均匀的路由里是否存在真正的输入驱动开关？
- **做法**：对每 (层, state token) 取平均概率最高的专家，看其实际概率分布形态；对夹爪开度
  （state[6]−state[7]，注意两指镜像、求和会抵消）做剂量-响应与 LOSO。
- **结果**：双峰开关（56% < 0.05，24.7% > 0.5，上模 ~0.8）；只在前块（2–5 层）存在，后块
  on-rate 0.0%；剂量单调（开度 <0.002 → 99.8% on；>0.072 → 0%），单阈值 94.1%（多数类基线
  75.3%）；bit 级噪声免疫。层位与极性 checkpoint 特异（同一权重不同任务主门在 L4/L5）。
- **分析**：连续策略里长出的符号开关，是 pose code 中最锐的一个 cell；用 AUC 而非 accuracy
  比较（on-rate 1.4–22% 时 accuracy 会制造伪"任务特异"）。因果检验见 §11。复制：3 checkpoint。

### 8. action token 路由 = 当前噪声 latent 的投影（8/21 机制重构）

- **思想**：动作 token 路由跨轮变化——它编码"未来动作"还是"当前计算状态"？
- **做法**：同观测 16 噪声候选 × 44 query，路由指纹（逐站点归一化 + Hellinger）与
  当前 latent 几何 / 即时更新 / 最终动作做（偏）Spearman，query 内置乱对照；state token 与
  AS 作零对照（应精确为 0）。
- **结果**：tau0 路由 ↔ 当前 latent 几何 rho **0.880**；控制当前 latent 后 ↔ 最终动作
  **−0.079**（零）；↔ 即时更新的偏相关 0.147→0.324（轮次递增）；相邻轮 top-1 保持率
  0.744→0.591（重组集中在末段）；零对照精确为 0。
- **分析**：动作 token 路由是**去噪计算路径的描述符**，不是提前选定的语义未来。与 §5 合起来：
  **两条通道、一个机制——路由 = 各 token 当前内容的 VQ**。scope：1 任务 4 rollout，机制推导
  跨任务成立的证据在 §25 的支配性检验。

### 9. 场景/任务信息：早期存在、随物理发散而失义

- **做法/结果**：512 rollout 上步 0 场景可识别性 100% → 步 13 后崩塌（"同场景"不再意味
  "同状态"）；30 任务语料 between/within = 2.80（等样本量校正后）；wrist 布局探针 1.000
  （p=0.0033）。
- **分析**：路由携带任务/场景身份，但内容仍是"状态坐标"——身份可分因为姿态分布可分。

### 10. 去噪轴：轮钟 + 末段加速

- **做法/结果**：30 任务全语料相邻控制步路由连续性比 0.866（p<1e-4）、lag 2–4 饱和；
  轮间路由变化随去噪轮单调放大（0.0184→0.0344）；直线几何（§24）下第 1–9 轮路由对第 0 轮
  几乎无新信息。
- **分析**：去噪轴上的路由主要是时钟读数；末段加速由平票带上的胞元边界穿越解释（见 §24）。

---

## 第三部分：因果实验

### 11. 钉住夹爪门：重掷实现、不动期望（最干净的因果结果）

- **思想**：唯一的输入驱动路由若被冻结，成功率应当变化——若不变，"路由是决策变量"被否。
- **做法**：三臂各 512 集（libero_spatial t05），(初始态, 噪声种子) 配对；state token 在层 2–5
  的路由分别冻到 on/off 两个 regime；干预落地核查（87–90% 轨迹改变、20 万+ 替换/臂、
  与原 top-4 平均重叠仅 1.2–1.5/4）。
- **结果**：成功率 97.66% / 96.29% / 98.44%，差异 CI 全含零（功效：2pp 单向掉落 94% 可检出）；
  但**哪些局成功被完全重掷：φ 1.000 → 0.106/0.085**——10/12 个基线失败被"救活"、17/500 个
  成功被"打死"，恰为统计独立的预期（11.6/18.6）。管线 bit 确定性使每个不一致对都可归因于干预。
- **分析**：路由对**轨迹实现**有完全权威、对**成功率期望**中性——信息是借来的，扰动只是重掷
  骰子。这是"causally consequential without being a readable decision state"的实证核心。
  未测：t08（57.8% 基线有双向空间）。

### 12. 步 11 路由移植：低功效 null + 不可引用的动作差

- **做法**：16 受体 × 5 臂，仅控制步 11 覆写全部 HB 路由，供体来自同进程实况；配对 McNemar。
- **结果**：全臂 null（donor_fail p=0.688 等）；self 臂精确复现 none（机器与环境无误）；
  首份报告的动作差 0.11–0.18 **无磁盘工件，不可引用**；n=16 只能检出巨大效应。
  幸存物：步 11 扰动能翻转结局（6/16、3/16）⇒ 该时点结局尚未定死。
- **分析**：保留为"极低功效下未检出"，不是"无效应"；若重做需存逐集数组。

### 13. 块与支路消融：整块是真机器，支路互为冗余

- **做法**：released-left 敏感操作点（28% 基线），同初始态+种子配对，forward-hook 替换；
  四臂：无/关 shared/关 routed/关整块。
- **结果**：随机化 router 32% vs 28%（p=0.83，null）；**关整块 10–14%**（首轮 p=0.049 复制轮
  p=0.143——方向可信、50 集不足定量，p=0.049 不可引用）；关任一支路无感（30%/24%）。
- **分析**：MoE 计算重要、路由选择不重要；支路"冗余"之谜的机制解见 §18。

### 14. 专家整组替换：专家不可互换，但支路小

- **做法**：固定权重只换身份：换最弱 1 个（第 5 名/随机）、换整组 4 个（第 5–8 名/随机 4）；
  Goal 测、Spatial 复制；淘汰离群敏感的 max 度量、改相对 L2。
- **结果**：swap4_random 动 **MoE 块输出 31.3%（Goal）/50.1%（Spatial）**、动最终动作
  6.0%/19.2%；第 5–8 名与随机 4 几乎等价（排序在边界下无功能含义）；与正交预测自洽
  （√2×支路占比 ≈35% vs 实测 31.3%）。
- **分析**："稀疏随机特征集合"被直接否证——**路由边界数值上纸薄、功能上厚实**；
  动作只动小是支路小（§18 三层稀释）。

---

## 第四部分：专家侧（分工的真正所在）

### 15. 专家是真函数、近正交基

- **做法**：在 hidden.zarr 的真实 h 上跑全部 32 专家，分解 E_e(h) = 均值 + 输入依赖残差。
- **结果**：输入依赖分量是常数分量的 **4–7×**；32 个均值输出方向两两 cos +0.011±0.038
  （d=1024 随机基准 SD=0.031），与 shared expert 同样正交；前块专家在被路由到的输入上
  写得更多（1.30× vs 后块 1.07×）。
- **分析**：专家 = 近正交函数基；"which expert"不是"which 固定向量"。

### 16. 槽位身份跨全部微调稳定（32/32）

- **做法**：cos(专家 e 此 checkpoint, 专家 e 彼 checkpoint) 在 1500–3000 个真实 h 上，
  对照不同专家的 +0.002±0.009。
- **结果**：Spatial 0.766 / Object 0.758 / Long 0.723 / **CALVIN-D 0.798**——四组对比
  **最近邻全部是自己（32/32）**，含跨模拟器跨动作空间；微调只原位旋转 20–28%。
- **分析**：专家基由异质预训练设定、微调不换槽——给专家点名（如 L2:e5）在全 release 合法，
  这是所有跨实验对话的地基。

### 17. router 做幅值增益控制

- **做法**：corr(gate 的 pre-softmax 分数, ‖E_e(h)‖) 逐层，32 专家上。
- **结果**：前块 +0.31~+0.45（26–29/32 为正）；**L15 = −0.43（31/32 为负）**——前块路由给
  "将要写最多的"，末层路由给"将要写最少的"。
- **分析**：非语义分工，是幅值分配（前放大、后衰减）；与后块惰性的另三项测量（近均匀负载、
  gate 读低方差子空间、最小 routed/shared 比）四证合一。

### 18. routed vs shared 的通道分裂 + 三层稀释 + 后块末段醒来（本日，3/3 复制）

- **思想**："专家实际输出贡献小"到底是复制品（H1）还是被容忍的独立函数（H2）？
- **做法**：真实 h + 权威 top-4（存储 id + 重归一权重）fp32 重算 routed(h) 与 shared(h)，
  按层×token×去噪轮出范数比/余弦/块占比/线性冗余度（PCA-128 ridge 留出 R²）；
  Goal/Long/Spatial 三 checkpoint。
- **结果**：**state token：routed 比 shared 还大**（1.05–1.22×，占块 65–79%）但近线性
  （R² 0.93–0.97）；**action token：小（4–41%）而真非线性**（R² 0.17–0.55），且删除后块输出
  方向几乎不转（cos(块,shared) 0.92–0.999）；**后块 routed 在去噪末段醒来：L15 ~5%→25%、
  L12 ~20%→35%（d0→d9），三 checkpoint 逐位一致**。
- **分析**：消融悖论=三层稀释（份额小×方向被掩护×残差流稀释，块只占流 8–30%），
  单删三阶小量、双删拿掉 8 层全部 MLP；pose code 的载体是 state token 上那个大而平滑的
  routed 计算；后块专家的主战场在去噪最后几轮（gripper 精修区）——与截断线直接挂钩。

---

## 第五部分：应用尝试与边界（每条附死因）

### 19. 结局预测：承诺是分数不是索引，路由不超越状态

- **做法**：16×8 承诺网格（同前缀共未来噪声，384 格）；512 集 max-statistic 家族校正的
  feature×step 扫描；t00 kNN 残差控制；k=0 熵分解。
- **结果**：步 0 首步路由 16/16 行翻转、MI p=0.92（不锁结局）；结局按 episode 长度的 ~88%
  处大部解决（79–89%）；t08 上任何通道步 13 前无信号（家族 p=0.0005）；场景身份本身在 k=0
  锁 44.3%（CV 下界 38.2%）；t00 步 11–12 的"超越状态"降级为 suggestive（旧管线、单初始态、
  事后挑控制规格、窗口坐在已决定的结局上）。
- **死因**：路由是姿态拷贝，H(Y|路由) ≥ H(Y|物理态) 恒成立。

### 20. 路由规则：无确定性规则；一条"提取差距"规则存活

- **做法**：256 文字×32,896 连词全扫（可评估 19,077），within-scene 置换的 max-statistic 定价
  多重性；诚实分割（挖/评不同场景，配对 rule−random）；位姿孪生；HistGBT 残差基线逐级加强
  （+proprio→+47 维全态→+动作块→**+40 维历史**）；seed-disjoint 复核；seed 主效应检验。
- **结果**：无高支持度纯失败规则（最纯 30 vs 零假设 95 分位 29——边缘，措辞按审计 A5）；
  `L2:e5&L5:e8` 族：诚实分割 +0.0108~0.0129±0.0035（16/20），**加 40 维历史不灭
  （+0.0120±0.0037）、seed-disjoint 存活（19/20）、无 seed 主效应（p=0.76）**；
  位姿孪生：union +11.9pp [4.5,19.1]（旗舰规则单独 CI 含零——A4 修正）；仅 t08，
  其余 3 任务 ≈0。
- **分析**：**有限容量基线的提取差距**——VQ 胞元边界是免费的学习特征，kitchen-sink GBT
  在预算内重构不出；不是"物理态之外的信息"（香农上界）。路由信息账本里唯一的真实存款。

### 21. RAD/剪枝、候选挑选、噪声选择：三重关闭

- **做法/结果**：label-free 密度剪枝四 bundle 全负（最密路由盆保留失败）；监督探针可剪但读的
  是已决定分支；fork：within-snapshot 有信号（action 0.264 > route 0.112）、跨 snapshot 归零
  （结构性，非功效）；行动云几何/路由/噪声中心度选择器在 task+seed 双留出下全灭
  （11 方法 FWER 最好 0.083），**oracle：真动作中心度选择 = −0.002**——伪目标家族本身无价值；
  发现 seed-template 混淆（噪声中心度每 fold 只选 ~1–4 个固定 seed）。
- **死因**：候选价值是状态条件的（Q=f(o,s,phase,A)），候选云几何/路由里没有状态语义；
  oracle 为零处任何集成只能产出 winner's curse。

### 22. 失败预警/停摆检测：能报，但不如裸 proprio

- **做法/结果**：失败吸引子=手臂停泊（开爪、停动）；路由如实报告（驻留 ≥7 步的胞元 92.2%
  失败）但 matched-K 的 proprio k-means 在全部符号动力学指标上更强（I(c_t;c_{t+1}) 逐 K 全胜）；
  物理态更早分离（步 8 vs 13）。
- **分析**：可当停摆检测器用，但不能声称 router 学了动力学适配的划分。

### 23. 轨迹形状"优势"：审计判定为度量伪影（CLOSED）

- **做法**：把 proprio 经 LOSO RFF（R²=0.939）映入同一 256 维路由空间，在"纯姿态函数"的
  预测轨迹上量同款 path length。
- **结果**：纯姿态轨迹 AUC **0.848 > 真实路由 0.817**；路由残差轨迹控制夹爪开合后 0.495
  （p=0.91，纯零）；原"物理态 0.733"低是 47 维稀释（裸 proprio 0.800）。
- **分析**：优势属于 pose 嵌入的度量（分辨率集中于手臂），不属于路由信息。
  归属判定方法论：**给被对照方公平的表示能力**。

### 24. 去噪停止器：路由信号全灭；死因与幸存者的几何统一

- **做法**：9 个路由停止信号（轮间变化/瞬时统计/末轮模板距离/跨层一致性…）用同一把尺
  （与真实剩余误差的 Spearman）打分，参照免费的 ‖v‖·t；学习型组合 + 轮次消融；
  公平对局（latent 侧同等特征丰富度）；state-token 深度调度器 vs 控制步计数；
  上游窗口三连测（动量基线、前/后块增量、oracle 跳轮）；流直线度测量。
- **结果**：裸阈值全灭（相邻轮变化与收敛**反相关**，pooled −0.68——"等路由安静"等的是不会来
  的信号）；死信号组合可复活至 0.945（标定死非信息死）但公平对局下路由边际 **+0.001**；
  调度器 0.916 被"数轮次"0.910 打平；动量基线 0.997 无处增值、oracle 跳轮=0；
  **流路径近完美直线（相邻 v 余弦 1.000，指向终点 0.999）**——没有懒轮可跳、但有直线可抄，
  一跳外推 x̂=x−t·v 从第 1 轮即差 7.3%、r=5–6 差 4.2–4.8%（低于换 seed 扰动 0.049）。
- **分析**：路由作为停止信号被 latent 全面支配（VQ 的直接推论）；截断+外推（不用路由）是
  唯一幸存方案，t08 在线配对试点按冻结预注册进行中。"标定死可合葬复活、信息死加多少都是坟"
  ——判别法：先看 oracle 是否为零。

### 24b. OOD 哨兵（形态 C 雏形）：内部激活的第一个实用胜利

- **思想**：激活是输入的函数 ⇒ 关于世界无新信息；但作为**输入边界异常的通用报警器**，
  内部信号可能比行为面更快——这是"内部超越表面"唯一合法且此前从未测过的战场。
- **做法**：right50（正确布局，参考分布）vs left50（wrist 配置错误）共享 50 初始态；
  预声明信号：路由占用直方图 8×32（首推理）/首动作块 70 维/步 0 proprio（负对照，
  两臂同初始态理应无信号）；对角白化距离打分，n-平均检出率 @FPR≤5%。
- **结果**：**路由单次推理 AUC 0.990、1 次即 95% 检出**；首动作块 0.686、需 ~25 次；
  行为（成功率 28% vs 100%）需 ~3 集 ≈ 36+ 次推理；proprio 负对照 0.409 ✓。
- **分析**：内部比行为快 ~36×——VQ 的正面用法（输入坐标的量化码对输入端漂移即时响应，
  动作是任务收敛后的残影）。**诚实边界**：此 OOD 类型对内部最有利，且该特定错误用
  原始像素检查一行即 AUC 1.0——卖点是**通用性**（无需知道盯哪里）。旧管线数据，
  分布级比较不受影响。
- **特异性阶梯（同日扩展，新管线数据，`audit_ood_ladder.py`）**：
  L0 纯 MIG 切片扰动——三信号全部沉默（无假警报）✓；
  **L1 wrist-mask 位翻转（同切片、两臂 wrist 图均为全零、像素检查构造性失明）——
  路由 1.000/1 次推理，动作面 0.928/3 次，proprio 沉默** ✓——让项目损失两周的 bug
  一次前向可捕获；L1b（掩码+切片）与 L1 一致；L2 任务切换双方饱和（proprio 仅 0.628：
  手臂在 home 位，路由透过视觉前缀看见场景——action-token 路由携带前缀信息的直接旁证）。
  响应按语义严重性而非扰动大小排序——可部署遥测通道应有的形状。
  剩余最后一级（环境级 OOD：光照/干扰物/指令改写）待 GPU。

---

## 第六部分：理论综合与方法学

### 25. VQ 统一账户（机制理论）

**三个实测前提**：专家近正交（§15）+ 组合权重近平（§2，设计目标）+ 主损失支配 router 梯度
（aux/flow = 1–10%，首个真实 backward 测量）⇒ **top-4 集合 = h 在 32 个固定方向上投影幅值的
argmax 组 = 该 token 输入流形的 VQ 胞元编号**。

**已验证的推论**：离散 top-4 解码优于连续概率（0.892 vs 0.831）；跨任务可分性 2.80；输入更
丰富则纯度下降（96→72%）；胞元扰动结构鲁棒 ⇒ 全部因果 null；前 +0.45/末 −0.43 增益控制；
今日两裁决（path-length=度量伪影、规则=胞元边界提取差距）均落入框架。

**精确措辞**（经论辩修正）：路由是**计算过程的窗口——因果在上游（§11/13/14 的真机器），
信息不增值**（公平对局 +0.001）。不说"latent 的投影"。

**边界与外推**：这是对本 release/训练配方的账户，非 MoE 普遍定律；可检验预测——锐化路由
（低熵/强 z-loss/大 U）的 MoE 应更语义化；跨源异质训练期的路由（设计主战场）**未测**，
一切结论带"部署期、单源内"scope。

### 26. 方法学 battery（凡引用路由结果必携带）

配对种子 + bit 确定性门控（新管线 sha256 级）｜置换 max-statistic 定价扫描多重性｜
位姿孪生与 matched-K 对照｜**公平对局归属**（给对照方同等表示能力）｜**oracle-first**
（先测真目标的天花板再造预测器）｜seed-连贯置乱与 seed-disjoint 分割（共享 seed 语料的
强制项）｜零对照内建（state token/AS 必须精确打出 0.500/0.0）｜端到端总 pass 记账｜
探索/确认两轮制，头条只出自确认轮｜三操作点（released-left / checkpoint-right / paper-right）
永不同表。

---

## 附：结果状态速查

| 状态 | 条目 |
|---|---|
| **正结果（发表主料）** | §5 pose code、§6 坐标跟随、§7 夹爪门、§11 重掷不改率、§15–18 专家侧全套、§25 VQ 账户 |
| **正结果（次级）** | §8 latent 投影、§20 提取差距规则、§24b OOD 哨兵雏形、§9/10 结构描述 |
| **设计确认（不作发现宣称）** | §1 AS 常数、§2 负载均匀 |
| **关闭（附死因）** | §19 结局预测、§21 选择/剪枝、§23 轨迹形状、§24 路由停止器 |
| **降级/禁引** | t00 步11-12（suggestive）、移植动作差（无工件）、p=0.049（未复制）、pre-action 协议（未运行） |
| **未测** | 跨源训练期路由（设计主战场）、t08 上的门控 pin、幅值类数字的 GPU 复核 |
