# HiMoE-VLA 路由状态：同一场景 64 次 flow 噪声

日期：2026-08-09
模型：HiMoE-VLA-Libero-Goal，`checkpoint_sha256 = 98ee29d09d1855716e341532a3d0f76068de6fa131f8fe16ffd52f982df1b953`
代码：`himoe-libero-wrist-fix` @ `d779741`，采集与分析在 `/home/jovyan/work/himoe-route-capture/`
硬件：H20-3e 的 MIG `2g.35gb` 分片（16 SM / 32.5 GiB）— **与前一份报告的 `1g.35gb` 不同，见第五节**
前序报告：[`himoe-vla-moe-routing-analysis-2026-08-07.md`](himoe-vla-moe-routing-analysis-2026-08-07.md)

---

## 一句话结论

把重复次数从 10 提到 64、固定在一个有动态范围的初始状态上（`released-left` + init state 24，
成功率 **23/64 = 35.9%**），得到一个前一份报告做不到的干净判据：

**控制步 0 上，路由状态对最终成败没有任何信息。** 此刻 64 次 rollout 的观测逐字节相同，
唯一的差别是 flow 噪声。留一交叉验证的平衡准确率是 **0.423**（置换检验 p = 0.92），
低于随机。8 个 HB 层单独看也都在 0.39–0.51 之间。

到第 14 个控制步，同一批路由状态已经把成败分得很开（组间/组内分离度从 0.29 升到 1.05，
用前 17 步做解码是 **0.898**，p = 0.001）。但那时轨迹早已实际分岔——这是**事后读出**，
不是**提前预警**。

作为对照，同一批数据里"免费的 flow 集合"也不行：步 0 的 64 个动作块相对集合均值的偏离量，
预测成败的 AUROC 只有 **0.577**。**两条路线在步 0 上都是空的。**

---

## 一、这次跑了什么

| 项 | 值 |
|---|---|
| 任务 | LIBERO-Goal task 0，`open the middle drawer of the cabinet` |
| wrist 布局 | `released-left`（错误槽位，基线 28%——选它是为了有动态范围） |
| 初始状态 | **固定为 24**（旧 10 次批次里是 4/10，最接近 50%） |
| 重复次数 | **64**，flow noise seed 1000–1063 |
| 其余协议 | 环境 seed 7、settle 10 步、replan 10、上限 300 步（沿用已审计设置） |
| 采集 | `serve_with_recorder.py --store-full-probs`，完整 32 维 softmax |
| 产物 | 1682 个控制步、形状 `[1682, 8, 10, 11, 32]`、68 MB |

**为什么固定初始状态很关键。** 前一份报告第六节要预测成败，必须先把场景难度减掉
（同一 init state 重复 10 次、按 state 去均值、组内置换），因为 8 个不同场景的难度差异
会盖过一切。这里 64 次全在同一个 init state 上，**场景按构造就是常量**，不需要任何减法。
更重要的是：控制步 0 的观测在 64 次之间逐字节相同（同一 reset、同样 10 步 settle、
环境确定性），所以步 0 的任何路由差异都只可能来自 flow 噪声。

**这一条是实测的，不是推断的。** 64 集在控制步 0 的 8 维机器人状态
`max |state_i − state_0| = 0.0`，逐位相同；同一时刻的动作块则不同
（`max |a_i − a_0| = 0.062`，7 个维度的跨抽样标准差
`[0.0138, 0.0089, 0.0166, 0.0021, 0.0034, 0.0042, 0.0123]`）。
图像来自同一个 MuJoCo 状态，渲染是确定性的。

采集开销 **2.57 ms / 控制步**，占推理时间（717 ms）的 0.36%。
AS 折叠断言在 **1682/1682** 个控制步上全部成立，独立复核了前一份报告第二节
"AS-MoE 在 LIBERO 上是常量路由器"的结论。

---

## 二、步 0：路由和动作集合都预测不了成败

留一交叉验证 + 1000 次标签置换零分布。特征先做无监督 PCA 降维（不用标签，
置换零分布继承同一投影，所以不构成泄漏）。

| 步 0 特征 | 原始维度 | 平衡准确率 | 置换 p | 零分布均值 / p95 |
|---|---:|---:|---:|---|
| 动作 token，8 层全部 | 2560 | **0.423** | 0.92 | 0.509 / 0.612 |
| state token，8 层全部 | 2560 | **0.500** | 1.00 | 0.500 / 0.500 |
| 只用 router 熵 | 880 | **0.452** | 0.78 | 0.500 / 0.615 |

逐层也都是空的：层 2 / 3 / 4 / 5 / 12 / 13 / 14 / 15 分别是
0.510 / 0.435 / 0.469 / 0.494 / 0.435 / 0.389 / 0.435 / 0.501。

**同一批数据上的 flow 集合对照。** 步 0 的 64 个动作块（`[10, 7]`，同一观测、
不同噪声）就是 K=64 的集合。每个样本相对集合均值的偏离量用来预测成败：

| 量 | 值 |
|---|---|
| 平均偏离 | 0.0852 |
| 成功组 / 失败组 | 0.0864 / 0.0845 |
| AUROC | **0.577** |

两组几乎重合。**这正是上一轮讨论里"路由探针要赢的对照"——结果是两边在步 0 都赢不了，
谁也没有可用的早期信号。**

---

## 三、什么时候才分得开：第 10–14 步

把解码窗口从步 0 往后拉（每集都取等长前缀求平均，所以不含长度混淆）：

| 窗口 | 可用集数 | 平衡准确率 | 置换 p |
|---|---:|---:|---:|
| 前 1 步 | 64（成功 23） | 0.423 | 0.89 |
| 前 3 步 | 64 | 0.583 | 0.117 |
| 前 5 步 | 64 | 0.515 | 0.47 |
| **前 17 步** | 64 | **0.898** | **0.001** |

群体几何量给出同样的时间点（成功组/失败组路由中心距离 ÷ 组内散度）：

| 控制步 | 0 | 5 | 10 | 14 | 16 |
|---|---:|---:|---:|---:|---:|
| 分离度 | 0.285 | 0.214 | 0.321 | **1.052** | 1.042 |
| 集合散开程度 | 0.0164 | 0.0175 | 0.0429 | — | 0.0704 |

**第一个结束的集在第 17 步**，所以 10 → 14 这段跃升发生时 64 集全部还在运行，
不是幸存者偏差。但到第 14 个控制步已经走了 140 个环境步，抽屉开没开基本已成定局——
路由此时反映的是已经发生的分岔。

这把前一份报告第六节的"路由是读出不是控制"收紧了一步：**它是一个晚的读出。**

---

## 四、路由本身的统计量（64 集独立复核）

| 层 | 熵/ln32 | 有效专家 | top1/均匀 | top1−top2 | top-4 质量 |
|---|---:|---:|---:|---:|---:|
| 2 | 0.9885 | 30.7 | 1.66× | 0.0123 | 0.164 |
| 3 | 0.9863 | 30.5 | 1.72× | 0.0123 | 0.167 |
| 4 | 0.9808 | 29.9 | 2.01× | 0.0248 | 0.170 |
| 5 | 0.9863 | 30.5 | 1.75× | 0.0153 | 0.166 |
| 12 | 0.9975 | 31.7 | 1.26× | 0.0019 | 0.149 |
| 13 | 0.9974 | 31.7 | 1.27× | 0.0022 | 0.149 |
| 14 | 0.9978 | 31.8 | 1.25× | 0.0020 | 0.147 |
| 15 | 0.9972 | 31.7 | 1.33× | 0.0045 | 0.150 |

与前一份报告（`checkpoint-right`，312 控制步）的 0.9842–0.9981 / 30.3–31.8 一致。

**top-4 集合 Jaccard**（逐路由站点，随机 4/32 基线 0.067）：

| 层 | 相邻控制步 | 跨噪声 @ 步 0 | 跨噪声 @ 步 1 |
|---|---:|---:|---:|
| 2 | 0.250 | 0.330 | 0.299 |
| 5 | 0.270 | 0.332 | 0.318 |
| 12 | 0.308 | 0.513 | 0.503 |
| 15 | 0.300 | 0.562 | 0.548 |

相邻控制步 0.250–0.308，复现前一份报告的 0.16–0.22 量级（这里略高，因为固定了初始状态）。

**一个新的比较**：跨噪声（同一控制步、不同 flow 噪声）的 Jaccard **高于**相邻控制步。
换一次噪声对路由选择的扰动，比场景往前走一步还小；晚层（12–15）尤其如此，0.51–0.56。

**第三条轴：一次推理内部。** 10 个去噪步之间的相邻 Jaccard 是 0.57–0.85，
远高于控制步之间的 0.25–0.31——路由在 flow ODE 积分过程中相对稳定。
残差幅度随去噪步呈 U 形，t=4–5 最小、t=9（最接近动作）最大。这条轴前一份报告没有画过。

---

## 五、复现性：换了 MIG 分片，结果不再逐位相同

旧的 10 次批次跑在 `1g.35gb`（12 SM）上，本次跑在 `2g.35gb`（16 SM）上——原分片被其它租户
占了 22 GB。重叠的 10 个 seed：

| | 一致 |
|---|---|
| 成败结论 | **10 / 10** |
| 步数完全相同 | **7 / 10** |

不一致的三个：seed 1000（168 → 184 步）、1003（225 → 177）、1006（174 → 164），
成败都没变。**SM 数量变化会改变 cuBLAS 的 kernel 选择，轨迹因此不是逐位复现的。**
本批 64 集内部是自洽的（同一分片、同一进程），但不能与旧批次做逐步配对比较。

需要严格配对时，必须把 MIG profile 固定下来并记录。

---

## 六、可视化产物

| 文件 | 说明 |
|---|---|
| `himoe-route-capture/viz/himoe-routing-viewer.html` | **单文件交互页面（2.8 MB，双击即开）**，6 个联动面板 |
| `himoe-route-capture/viz/index.html` + `data.js` | 同一页面的分体版，供本地 HTTP 服务用 |
| `figures/within64-s24/routing_evolution.mp4` / `.gif` | 路由状态随控制步演化的动画 |
| `figures/within64-s24/f1_router_is_flat.png` | router 有多平（有效专家 30+/32） |
| `figures/within64-s24/f2_state_over_time.png` | 专家 × 控制步残差热图，成功 vs 失败 |
| `figures/within64-s24/f3_ensemble_spread.png` | 群体散开、存活集数、成败分离度 |
| `figures/within64-s24/f4_piano_roll.png` | top-4 选择的翻搅 |
| `figures/within64-s24/f5_denoise_axis.png` | 去噪步轴上的路由变化 |
| `analysis/within64-s24/analysis.json` | 全部数值 |

交互页面里最直接的一屏：面板 ① 把 64 次抽样投影到共同的主成分平面，带从第 0 步到当前步的
轨迹尾迹。**64 条轨迹从同一个点出发**（步 0 观测相同），沿几乎相同的路径走过前 10 步，
在第 10–14 步之间分成两簇。

所有面板画的都是**减去各专家自身均值后的残差**——原始概率贴着 1/32，不放大看不见任何东西。
页面里有"原始概率"开关，切过去就能看到它有多平。

---

## 七、限制

1. **单初始状态、单任务、单 checkpoint。** 全部 64 集都是 Goal task 0 的 init state 24。
   步 0 的零结论不应外推到别的场景——尤其不能外推到"模型没见过的 OOD 输入"，
   本实验里 64 次的输入完全相同，测的是噪声引起的差异，不是分布偏移。
2. **步 0 的零结论是"看不出"，不是"证明没有"。** n=64（23 成功 / 41 失败），
   置换零分布 p95 = 0.612，所以本实验只能排除"平衡准确率高于 0.61"的效应。
   要排除更小的效应需要更多集数。
3. **第 14 步的 0.898 有天花板成分。** 前 17 步的均值特征里，靠后的步已经能看到抽屉状态，
   所以它更接近"读出当前进度"而不是"判断策略质量"。
4. **动作集合对照只测了偏离集合均值这一个标量。** 更强的集合判据（例如按维度归一化、
   只看夹爪维、或用集合方差而非单样本偏离）没有测；步 0 的 AUROC 0.577 只否证了最朴素的那种用法。
5. **与旧批次不能逐步配对**（第五节）。
6. AS 层未做任何消融，本次只验证了它的常量性质。

---

## 八、可以直接接着做的

- **把同样的 64 次协议搬到 OOD 输入上。** 步 0 的零结论是在"输入完全相同"的条件下得到的，
  这恰好是路由探针最没有优势的设定。真正该测的是分布偏移：换光照、换干扰物、换语言指令，
  看路由状态能否在步 0 分辨"见过 / 没见过"。这是上一轮讨论里探针唯一可能赢过集合的地方。
- **把集合判据做全**（限制 4），再和路由探针在同一批数据上比 AUROC。
- **换初始状态重复本实验**，确认步 0 的零结论不是 init state 24 特有的。
- 前一份报告第十节列的消融扩展（Spatial / Object、单独消融 shared 分支）仍未做。

---

## 九、后续（2026-08-12）：路由减枝的三只时钟

这一节回答一个和前八节不同的问题。前面测的都是**解码**：在控制步 t 上拟合一个探针，
看它能不能分开 64 个结局。RAD 式的减枝要的是另一回事——它**不用标签**，只保留内部状态
云里最密集的那一撮候选。0.85 的解码和一条没用的密度规则完全可以并存。

把每个控制步当成一次减枝决策：64 条 partial rollout 都还活着，只用当步及之前的信息给它们
打分，留下前 B 条，看留下的那批里成功的比例比基础率高多少。

**幸存者偏差是这里的头号陷阱**：成功集 17–21 控制步，失败集跑满 30 步，所以只要过了第一条
episode 的退出点，"还活着"本身就是标签。所有扫描都截在 64 条全活的最后一步
（t0s24 与 within64-s24 ≤ 16，t3s0 ≤ 15，t1s19 ≤ 7）。

方案原本把 `KeepSuccess@B`（减枝后还留着至少一条成功）定为主指标。在 27/64 下，**随机**减到
B=8 就有 99.1% 的概率留住成功，B=16 是 100.0%——B>4 以后它分不开任何方法。主指标改成
`Enrichment@B = precision@B − 基础率`，并按随机减枝零分布的标准差归一成 z。

### 9.1 t_R：减枝什么时候开始留住成功

四个 bundle，规则一次冻结后一起跑。整个 (step × budget) 网格上 |z| 最大的那格，
FWER 已经为这次搜索付过账：

| 打分方式 | t0s24（发现集） | within64-s24（同条件独立复现） | t3s0 | t1s19 |
|---|---|---|---|---|
| **路由 / 监督探针** | **+5.78** p=.001 | **+5.52** p=.001 | **+6.12** p=.001 | +3.53 p=.110 |
| 世界状态 / 监督探针 | +5.78 p=.006 | +5.52 p=.007 | +6.12 p=.001 | +4.62 p=.028 |
| 动作块 / 监督探针 | +4.27 p=.029 | +5.43 p=.001 | +6.12 p=.001 | −3.15 p=.219 |
| **路由密度（RAD，无标签）** | **−3.33** p=.029 | **−3.43** p=.011 | **−3.43** p=.015 | −2.99 p=.088 |
| 路由 medoid（A³ 式共识） | +4.21 p=.001 | −3.43 p=.011 | +4.33 p=.001 | −3.92 p=.003 |
| 3 步路由窗口 / 密度 | −2.75 p=.188 | −3.43 p=.012 | −2.83 p=.172 | −2.99 p=.068 |
| 路由扣掉世界状态 / 监督 | +4.27 p=.026 | +3.73 p=.097 | −3.36 p=.235 | −3.29 p=.142 |

对照臂正常：`random` 在四个 bundle 上的 max |z| 是 0.20 / 0.16 / 0.22 / 0.22，
`oracle` 在 t0s24 上 B≤27 时精度恰好 1.000、B=32 时恰好 27/32=0.844。

**逐步看，t_R ≈ 10。** B=16 时路由监督探针的 z（两个同条件 bundle）：

```
t0s24         0..9 全在 ±1.6 内   t10 +2.5   t11 +4.2  t12 +3.6  t13 +3.6  t14 +4.2
within64-s24  0..9 全在 ±1.6 内   t10 +3.1   t11 +4.9  t12 +5.5  t13 +3.7  t14 +3.7
```

两条独立捕获都在**第 10 步**打开。世界状态臂第一次越过 z=2 分别在第 11 步（t0s24）和
第 12 步（within64-s24）——**路由领先物理状态一到两个控制步**。

操作点（within64-s24，基础率 0.359）：在第 11 步把 64 条减到 16 条，精度 0.359 → **0.875**，
花掉满额算力的 56.4%；减到 8 条同样是 0.875，花 49.1%。

### 9.2 但纯 RAD 不成立

**无标签的密度规则一条都不能用。**

- 路由密度在四个 bundle 上的最强格**全是负的**，其中三个 FWER 显著（p=.011/.015/.029）。
  "留下最密集的一撮"系统性地留下的是失败。
- 路由 medoid、状态密度、动作密度**跨 bundle 变号**（+,−,+,− 之类），没有一个方向能复现。
- 网格整体的倾向做置换检验后**都不显著**（路由密度 mean z = −0.25/−0.27/−0.30/−0.26，
  p = .26/.40/.23/.34）。所以正确说法是"没有可用的无标签规则"，
  不是"密度反着用就行"——反着用是拟合出来的方向，而且它连系统性都没达到。

所以方案第七节那个判据的答案是**否**：

> 路由可以被监督探针解码 ≠ 成功轨迹自己形成最密集的 basin。

同样地，方案押注的"信息在路由**动力学**里、3 步窗口能救活单步"也不成立：3 步窗口臂在四个
bundle 上全是负的，比单步更差。而单步路由在第 11 步本来就不是 null（原始 0.853），
所以那个前提一开始就不对。

还有一条要说清楚：**世界状态的监督臂和路由一样强**，而"路由扣掉世界状态"之后只在
t0s24（p=.026）和 within64-s24（p=.097，边缘）的第 11–12 步上还有东西，t3s0 与 t1s19 是零。
路由相对物理状态的增量是个**两步窗口**，只在四个 bundle 中的两个上复现。

### 9.3 t_B：候选确实会分开，而且推理是逐位确定的

用完整 sim 状态而不是观测里的末端位置重算（观测比 sim 落后一个子步，正是当初把噪声底
抬到 3 毫米、导致"候选池退化"结论被撤回的原因）：

- **同一恢复观测 + 同一 flow noise 张量，连发 8 次，action chunk 逐位相同（0.0e+00），
  三次探针合计 13 个相位全是。** 推理没有 GPU 非确定性，所有噪声底都可以按字面读。
- 候选落点的 qpos 离散度 5.8e-3 ~ 1.3e-2（均值），最大两两距离到 1.6e-2；
  分叉复现底在硬恢复下是 0.0e+00。信噪比十几个数量级，**t_B 在接触相位之前就已经打开**。

**但分叉机器不是无条件可信的。** 用当初 diversity 筛查那组种子复跑，第 6 相位的软恢复漂移
1.97e-2，比候选离散度 1.31e-2 还大；`sim.reset()`（清 `efc_` 与接触对缓存）能把它压到 0，
但第 12 相位软、硬、重放三种恢复全都失败（5.1e-3 / 5.1e-3 / 4.5e-3）。这不是恢复模式的问题
而是**状态特有**的：换一组种子、前缀干净时，同样的相位软恢复也只有 5e-15。

结论是：分叉必须**逐快照**验，不能一次验完就当全局成立。fork_pilot 现在每个快照都跑一次
重复探针并把漂移记进结果，实测第一个快照 0.0e+00。

### 9.4 t_C：结局在第 11–12 步基本已定（384/384 完整）

固定前 k 个控制步的完整前缀，只换后续噪声流，8 行前缀 × 8 条共同未来，六个 k 共 384 格，
全部跑完。完整性审计通过（`valid: True`）：初始观测哈希 1 种，同前缀的 AS/HB 路由、状态、
动作最大绝对差**全部恰好 0.0**（336 对），嵌套前缀比较 120 对全 0.0，3245 个未来噪声张量
0 处不匹配，分叉前成功 0 格。

| k | 混合行 | 锁死行 | H(Y) | H(Y \| 前缀) | 已解析 | 互信息置换 p |
|---|---|---|---|---|---|---|
| 4 | **8/8** | 0 | 0.989 | 0.906 | 8.3% | 0.211（不显著） |
| 8 | 6/8 | 2 | 1.000 | 0.601 | 39.9% | 1e-4 |
| 10 | 5/8 | 3 | 0.942 | 0.596 | 36.8% | 5e-5 |
| 11 | 2/8 | 6 | 0.954 | 0.203 | **78.7%** | 5e-5 |
| 12 | 1/8 | 7 | 0.928 | 0.101 | **89.1%** | 5e-5 |
| 14 | 2/8 | 6 | 0.913 | 0.193 | 78.9% | 5e-5 |

第 4 步前缀对结局**没有任何信息**（p=0.211，和步 0 的 16×8 网格结论一致）。到第 11 步
8 行里已有 6 行锁死（2 行全败、4 行全成），第 12 步 7 行锁死。

**这把 9.1 的窗口压得很窄。** 路由第一次越过 z=2 是第 10 步，那时结局确实还开着
（5/8 行混合，只解析掉 36.8%）。但路由信号最强的第 11–12 步（z=4.2–5.5），
**79%–89% 的不确定性已经没了**——探针在那里主要是在读一个已经定了的结果。

所以可用的窗口实际上只有**第 10 步前后一两步**，不是整个 10–16 段。列（未来噪声流）
方向的效应在 k≥8 时都不显著（p=0.08–1.0），所以行效应是前缀本身的，没有被列难度混淆。

### 9.5 同状态分叉：信息在，但不可迁移

20 个快照 × 32 个候选 = 640 条，每个快照重复探针的漂移**全部 0.00e+00**（漂移门槛没丢任何
快照；6 个快照因所有候选的抽屉位移完全相同而标签退化，自动排除）。

把两个必须分开的问题分开问：

| | 路由 | 动作块 |
|---|---|---|
| **快照内拟合**（信息在不在？） | ρ=**0.112** p=.0025 | ρ=**0.264** p=.0005 |
| **留一快照外拟合**（能不能做成选择器？） | ρ=0.010 p=.43 | ρ=−0.034 p=.77 |

两条读数：

1. **信息确实在。** 在固定状态下，路由和动作块都显著预测候选会做出什么。N=8 时问不了这个
   问题（拟合都拟合不了），N=32 可以——这一点上"加候选数"确实有用。
2. **但路由只是动作的弱代理。** 0.112 对 0.264：产生这条路由的那次前向传播输出的动作块，
   预测自己的后果比路由准 2.4 倍。**在固定状态下做 route critic 相对 action critic 没有增益。**
3. **而且都不能跨状态迁移。** 留一快照外对路由、动作、两者拼接全是零
   （零分布 p95 = 0.073，比上一轮 N=8 的 0.156 好一倍多）。所以瓶颈不是候选数，
   是"动作→后果"这张映射本身强烈依赖状态、学不到别的快照上去。

这一条**更正了上一轮的诊断**。N=8 那次我把动作基线为零读成"功效不足，把 N 提到 32"。
提到 32 之后，快照内确实出现了信号，但留一快照外仍然是零——所以那个零不只是功效问题，
它还是个结构问题：留一快照外要求模型在没见过的状态上预测一个动作的后果。

### 9.6 判定

```
t_B  ≲ 6   候选早就分开了（离散度比复现底高十几个数量级）
t_R  ≈ 10  监督探针能减枝，且比世界状态早 1-2 步
t_C  ≈11   第 4 步完全没信息；第 10 步解析 37%；第 11-12 步已解析 79%-89%
```

`t_B ≤ t_R < t_C` 成立，但**边界很紧**：三只时钟没有一段宽敞的重叠，只有第 10 步
前后一两步。方法主张必须改两处：

> **触发靠动作分歧、减枝靠无监督路由一致性——不成立。** 无标签密度规则在四个 bundle 上
> 一条都不能用（9.2）。
>
> **能用的版本**：在第 10 步用一个小的**有监督**路由 critic 把 64 条减到 16 条，
> 精度 0.36 → 0.88，算力约 56%。

这个版本的价值有多大，三条限定说了算：

1. 世界状态的监督臂**一样强**，路由唯一属于自己的东西是**早 1–2 步**；
2. 路由扣掉世界状态后只剩一个两步窗口，四个场景只复现两个（9.1）；
3. 到第 11–12 步——也就是路由信号最强的地方——**79%–89% 的结局不确定性已经没了**（9.4）。
   所以那个 0.88 的精度里，大部分是在读已经定了的分支，而不是在未定的分支之间做选择。
   真正"既有信号又还没定"的，只有第 10 步附近。

合起来：这条路不足以支撑"MoE 路由自带一个免费的 verifier"这个主张。它支撑的是一个弱得多的
陈述——**路由是一个比物理状态早一两步、但比动作块弱的进度指示器**。

### 9.7 这一节改掉的东西

- `object_control.py --all-qpos` 之前把物体自由度算了两次（57 维而不是 41 维），
  那一列与其它三种对照不可比。修好后第 11/12 步的 −both 是 **0.698 / 0.784**（原报 0.680/0.816），
  结论不变。
- `fork_pilot.py` 之前用回放循环里的 live observation 建查询，而恢复后的观测与它差 190 灰阶
  （52.5% 像素）。已改为从恢复取观测。
- 减枝分析里一个只在退化格才会暴露的统计 bug：z 用**四舍五入后**的 enrichment 算、零分布用
  未舍入的，两者恰好相等时零分布永远够不着观测值，p=1.0 被报成 1/(1+n)。已改为全程未舍入。

- `fork_analyze.py` 的置换检验原本每个排列都重解一次 ridge；ridge 对 y 是线性的，
  已改成预计算线性算子（与直接求解逐位一致到 4e-15），并补上逐快照的漂移门。

产物：`prune_curve.py` / `analysis/prune-curve/*.json`、`probe_fork_fidelity.py` /
`analysis/fork-fidelity/*.json`、`fork_pilot.py` + `fork_analyze.py` /
`analysis/fork-pilot-n32/fork_analysis.json`、
`fig_three_clocks.py` / `figures/three-clocks/three_clocks.png`。
