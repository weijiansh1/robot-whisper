# 执行记录

正式采集使用推理前冻结的协议、源码、输入和随机流，没有修改控制参数。

首个状态曲线图的纵轴说明过长，视觉检查发现相邻面板文字拥挤。
仅缩短坐标轴标题并将解释移入图标题，重新绘制该状态曲线。
原图以 `plus-task00-init026/component-trajectories-layout-v1.png` 和 `.pdf` 保留；
原始数值、采集实现、终局图及模型调用均未因此改变。最终绘图源码随 run 封存。

总览图首次使用本轮最大步差作为色标界限，会把正负 1 步显示为极端颜色。
改为至少正负 10 步的色标范围；原图以 `rollout-overview-scale-v1.png` 和 `.pdf` 保留，所有标注数值不变。
