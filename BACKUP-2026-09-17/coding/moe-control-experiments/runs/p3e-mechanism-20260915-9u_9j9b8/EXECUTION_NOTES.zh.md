# P3e 执行记录

## 采集

- 在新目录中冻结 15 个输入及 138 个来源哈希; 旧 P3d 产物只读校验。
- 使用隔离模型进程，原权重、原精度、原 16,384 MiB 上限。共享服务仅读取 metadata，没有发推理请求，没有重启。
- 正式一次采集完成 105 次前向，没有加载失败、缺失响应、补选状态或超预算重试。
- 新增 hooks 只保存张量副本，不返回替代张量。每请求结束检查 hook 集合恢复原状。
- 实际第一去噪步 L12 的局部输入、共享输出、残差一致; 后续位置允许传播后的输入变化，不混淆比较条件。

## 物理状态读取

- 分别在原 LIBERO-PRO / LIBERO-plus 环境初始化相同任务，重建 native 与 mobility_balance 的保存状态。
- 全部 4,815 个状态成功，未调用 environment.step。没有新增物理续跑、没有新增 rollout 成功率。
- 五个读取进程各自写独立目录的 LIBERO 配置，未修改原环境配置或原数据。
- PRO 读取约 5.78 秒; 四个 Plus 场景约 17.18--17.53 秒。部分读取与模型采集时间重叠，不将两者简单相加为墙钟耗时。
- 夹持使用原 robosuite 的双指接触判据，目标条件使用原 BDDL 谓词; 不是新增奖励或运行时控制输入。

## 分析与绘图

- 完成采集后，从磁盘独立复核真实 gate 算术、分配、监测值、候选筛查及新增激活重建。
- 后处理补充固定残差下的绝对差异检查，防止将不同分母下的相对百分比解释成信号消失。
- 第一版 task-timeline 的两个长纵轴标题过于接近，修改为短的双行标题。保留原 PNG/PDF，修订版为 task-timeline-v2，原数据未变。
- 未重跑模型或环境来生成图表，未删除或覆盖原版本。
- 64 项测试在采集前通过; 最终测试输出另存 tests.txt。

## 解释边界

15 个状态来自五个开发父场景，不是独立留出集; 其中十个状态按 P3d 已接受候选的时间选取。
前向筛查 15/15 不代表成功率，45 个不同非零探针没有新的环境结局。
第 120--170 步的物理停滞定位是采集后描述，不是预注册的自动失败标签或不可恢复判定。
三种不同方向的动作幅度接近，不意味着动作方向、后续收益或风险相同。
