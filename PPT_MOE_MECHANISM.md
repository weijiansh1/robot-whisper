# MoE 路由与 hidden：从"能不能报警"到"模型内部在做什么"

> PPT 大纲 · 2026-09-01
> 语料 A = `runs/rolling-star-a100-long-t08-k16-20260828`（352 分支）
> 语料 B = `VLA_MUI_HUB/.../KITCHEN_SCENE8_.../right-16x32`（512 集）
> 同一任务 `put both moka pots on the stove`、同一 checkpoint（sha256 `cdc2b21f…`）

---

## ⚠️ 给讲者：这份大纲改了你原来的弧线

你草拟的是「传感器贵 → 动作幅度不泛化 → **所以 MoE 好**」。
**第三步在检测口径上撑不住**，盘上数据是反的：

- routing 在完整 cached physical state + action 之外增量 `+0.008 / −0.014`，实用阈值 `+0.05`（go/no-go 8-28）
- trap onset lead 1–4：`physical_action` AUC **0.907**，加 MoE 变 0.896–0.902（**负增量**）
- 而且"不泛化"这条对 MoE **同样成立**：整初态留出后回到机会线

如果照原弧线讲，第一个懂行的人问"你和动作基线比过吗"就会当场塌掉。

**下面用的是能站住的版本**：MoE 的价值不在检测，在于它是这些信号里**唯一既能读又能写**的那个 —— 它是模型上的执行器，不只是传感器。这条更强，也是真的。

---

## Slide 1 · 问题

**能不能直接从模型内部看出「当前这条 rollout 已经出问题了」？**

任务：LIBERO-long SCENE8，把两个摩卡壶放上灶台。
512 集里 296 成功 / 216 失败；失败中 84.7% 是 loop 或停滞。

---

## Slide 2 · 三条路，先谈成本

| 信号 | 部署时拿得到吗 | 成本 |
|---|---|---|
| **物体位姿 / 目标距离**（`sim_state`） | ❌ 仿真特权信息 | 真机上要外部动捕，贵 |
| **本体感觉 + 动作 chunk** | ✅ 机器人自带 + 模型自己的输出 | **免费** |
| **MoE 路由**（`hb_router_probs`） | ✅ 前向过程内部 | **免费** |
| **hidden**（`hb_hidden`，gate 输入） | ✅ 前向过程内部 | 免费，但占 hub 96% 字节 |

> 讲者注：这里要诚实 —— **动作 chunk 也是免费的**。MoE 在成本上并不比动作基线便宜，
> 它只比"物体位姿真值"便宜。别把成本当成 MoE 的卖点。

---

## Slide 3 · 检测口径：MoE 输了

判出率 `det = max(AUC, 1−AUC)`，组内配对（A 按 worker / B 按初态），置换定价。

| | A | B |
|---|---:|---:|
| MoE 路由变化率（深块 12–15 / d9 / action token） | **0.799** | **0.751** |
| 置换零分布中位 | 0.607 | 0.577 |

看着不错 —— 但**换成加性检验就没了**：

| 检验 | 结果 |
|---|---|
| routing vs 完整 cached state + action | `+0.008` / `−0.014`（阈值 `+0.05`） |
| trap onset lead 1–4，MoE 加到 physical_action 上 | 0.907 → 0.896–0.902 |

**结论：要做报警器，用动作基线，别用 MoE。**

---

## Slide 4 · 三条路都不泛化

| 检验 | 结果 |
|---|---|
| 熵信号，整初态留出（LOSO / 4-fold） | 回到机会线（AUC 0.401–0.524） |
| 候选级 MoE 模型，留一 worker | Brier 0.3961 vs 基线 0.3313（**更差**） |
| 646 种切法，对基线残差后 | 全塌，唯一存活的要用标签 |

> 一个必须交代的细节：能贡献组内配对的组比名义上少 —— 单结局组直接出局。
> **A 实际 3 组、B 实际 13 组**（不是 4 / 16）。A 上的留一组检验分辨率很差。

---

## Slide 5 · 换个问题

检测是错的问题。**MoE 不是更好的传感器，它是研究对象。**

真正要问的是：路由里到底编码了什么？门控这一步在做什么？

---

## Slide 6 · 机理结果一：路由的动力学只有一个自由度

名义 → 有效的坍缩：

| 层级 | 数值（A / B） |
|---|---|
| 每步名义数字 `8×10×11×32` | 28,160 |
| 减去 softmax 约束后的自由参数 | 27,280 |
| **变化率读法下的有效维**（参与比 PR） | **1.81 / 2.25** |
| PC1 占方差 | 73.9% / 65.9% |
| `corr(PC1, action token 均值)` | **0.905** / 0.640 |

**646 种切法之所以都测到同一个东西，是因为它们全都是 PC1 的投影。**

补一条恒等式（已数值验证，偏差 2e-08）：
`all11 ≡ (10·action_all + 1·tok00) / 11` —— 所以"action token vs 整体 MoE"这个对比
本来就被算术绑死，上限只有 1/11。真正的证据是逐 token 拆分：11 个 token 的 det 挤在
0.73–0.82（A）/ 0.71–0.76（B），残差后**没有任何一个在两批同时存活**。

---

## Slide 7 · 机理结果二（今天的新结果）：门控丢掉了 99.4%，而丢掉的部分同样知道结局

**gate 是精确的秩-31 线性投影 + softmax。** 已验证：

| | |
|---|---|
| `logit ~ linear(hidden)` 的 R² | **0.9997**（残差是 fp16 存储噪声） |
| `rank(W)` | **31**（= 32 个 logit 减 softmax 常数方向） |

所以 1024 维输入被解析地劈成两半：**31 维 router 能响应**，**993 维 router 完全看不见**。

在 44 个 (层, token) 位点上问同一个问题（B 语料，t=30，512 集，组内配对 + 置换）：

| | router 可见的 31 维 | router 丢弃的 993 维（取前 31 PC） |
|---|---:|---:|
| 承载 h 的方差比例 | **0.57%**（中位） | 99.43% |
| det 最大值 | 0.829 | 0.752 |
| 有显著维的位点 | **44 / 44** | **44 / 44** |
| 丢弃半边 det 更高的位点 | — | **10 / 44** |

> **门控不是一个对齐了任务信息的瓶颈。它是一个近乎任意的低秩切片 ——
> 它扔掉的东西，在每一个位点上都同样知道结局。**

附带：state token（tok0）的可见方差比例是 action token 的约 3 倍（0.015–0.019 vs 0.005–0.008）。

⚠️ **这一页缺一个对照**：还没跑「随机 31 维投影」。
如果随机切片也给 det ≈ 0.70，那说明 gate 的 31 维**没有任何特权**，结论更强；
现在这一页只能说到"丢弃的部分同样有信息"，不能说"gate 选得不好"。**这是下一步第一件事。**

---

## Slide 8 · 所以 MoE 好在哪里

**不是"能不能写"。** 内部表征全都可写（这是 activation patching 的前提），
而且在仿真里连世界也可写 —— rolling-star 的 K=16 fork 就是靠
`full_state.npz`（`codec: himoe.full_state.safe.v1`）存下再恢复整个 sim state 做的。

真正的关系是**包含关系**，而 Slide 7 的秩-31 结果把它算准了：

> `route = softmax(W·h)`，`rank(W) = 31`
> ⟹ **一次路由干预恰好等于只编辑 h 在 31 维行空间里的分量**
> ⟹ **写路由 ⊊ 写 hidden**，且路由能碰到的只有承载 **0.57% 方差**的那块

区分两者的是**干预空间的形状**，不是可写性：

| | 干预空间 | 反事实有没有语义 |
|---|---|---|
| **MoE 路由** | 离散、可枚举（32 专家取 4） | 有：「改成走 expert 7 会怎样」 |
| **hidden** | 1024 维连续、无自然基 | 无：「在第 412 个方向加 0.3」不是一个问题 |

**一句话：信息在 hidden 里，标签在 routing 里。**
routing 是一个**小而有名字**的把手，装在一个**大而无名**的空间上；
秩-31 / 0.57% 就是这个把手的确切尺寸。

### 更根本的一条：干预是否留在流形上

任意改写内部表征会造出**模型永远到不了的状态** —— 那时你回答的是一个不存在的模型的问题。
按这条判据，三种干预分层：

| 干预 | 在流形上的程度 |
|---|---|
| **重抽 flow noise**（rolling-star fork） | **完全 on-manifold**：就是模型自己采样分布里的一个样本 |
| **pin 路由**（donor routing + 正确形式） | 每个 expert 仍收到真实的 h，只有门控被强制；`(这个 h, 那个 routing)` 的组合模型不会产生，偏离向下游轻度传播 |
| **patch hidden** | 任意向量写进残差流，可任意远离流形，且**没有"正确形式"可约束** |

`build_switch_pin.py` 的 docstring 已经把这条写死了：
> "…so the intervention lands on **routing the model actually produces, not on something invented**."
> pin 值 = 该 regime 平均路由的 top-4 + 重归一化概率，
> "**exactly the form the gate emits**"（`norm_topk_prob` 就是选中概率除以其和）。

**而 routing 之所以能被约束住，正是因为 `rank(W)=31`** —— 可达路由集合恰好是
模型 h 分布在 W 下的像，所以"这个 routing 合不合法"是个**可检验的问题**；
hidden 没有这样一个低维像可对照，这才是它难约束的真正原因。

### VLA 特有的安全网

**物理规则由环境执行，不由模型执行。** 不管内部被改成什么，MuJoCo 照常积分、
BDDL 谓词照常返回真实成败 —— 被干预的 rollout **永远是物理上合法的轨迹**。
（证据：rolling-star 352/352 条分支重跑原始 BDDL 谓词，记录与重放 **0 个不一致**。）

所以 off-manifold 的风险**在模型内部，不在世界里**。每次干预都白送一个
无歧义的 ground-truth 标签 —— 这是文本机理研究没有的条件。

> 讲者注：这一页原来写的是「MoE 是唯一能读也能写的信号」——**那是错的**，已改。
> 若有人追问"那为什么不直接研究 hidden"：不是因为 hidden 不能写，
> 而是因为 hidden 的干预**没有可检验的合法性判据**，routing 有。

---

## Slide 9 · 干预的现状：两个实验都打在了空处

**① 路由干预 `pin-*`**（层 2–5 路由被替换，95.8% 站点 top-4 改变）：

| 臂 | 成功率 |
|---|---:|
| pin-base | 500/512 = **97.7%** |
| pin-on | 504/512 = 98.4% |
| pin-off | 493/512 = 96.3% |

**跑在天花板任务上**（spatial/ramekin，基线 97.7%，只剩 2.3pp 下探空间）。
这不是"干预无效"，是**测不出来**。该跑的是 SCENE8（57.8%，上下各约 40pp 空间）。
另外 pin-* 各臂 `hook_verified_calls = 0`，干预是否生效只有间接证据。

**② 触发式 fork**：报警于 q32，fork 8 条救回 3 条 —— 看着成立。
但 `demo/rescue.json` 自己记了：**随机挑一个更早的步 fork 同样 3/8，初态 0 是 0/8**。
**救援成立，报警时机还没被证明有用。**

---

## Slide 10 · 下一步（按确定性排序）

1. **随机 31 维投影对照** —— 补齐 Slide 7 的最后一格，纯离线，今天可做。
2. **SCENE8 上重跑 `pin-*`** —— 把因果那一格从天花板挪到有空间的地方，补上钩子自校验。
3. **跨语料 PC 子空间对齐** —— A 上 PC1/3/4、B 上 PC1/2/3/4/10 通过家族校正，
   但 PC 编号跨语料不可比，必须把 A 的基底投到 B 上重测才知道是不是同一批方向。
   **限制**：只能在深块做（见附录 A2）。

---

## 附录 A · 两批语料不完全等价（讲者备查）

**A1 硬件不同 → 张量里有结构性差异**

A 跑在整块 A100-PCIE-40GB，B 跑在 MIG 切片 `2g.35gb`。
state token 的路由在 10 个去噪步上应恒定：

| | max\|e(d)−e(0)\| | 非零占比 |
|---|---|---|
| B | **0.000000e+00** | 0.00%（22,883 行 × 8 层全部） |
| A | 5.86e-02 | 3.10%（集中在前块 L3–L5；深块 ≤0.0039） |

→ **深块两批可比，前块不可比。** 且 state token × 去噪轴上 B 方差恒为 0，
A 的发现在那里**没有可比对象** —— 结构限制，不是功效不足。

**A2 时钟不同**

A 的分支在绝对 query 0/1/2/3/4/5 起跑（fork 点 = action step 0/10/20/30/40/50），
**288/352（82%）不是从 0 开始**。所以 A 的 "t=30" 横跨绝对 query 30–35，B 的就是 30。
本大纲所有数字都算在深块 12–15 / d9 上（两批一致到 0.002–0.004），结论不受影响，
但跨语料对齐必须先把 A 改成绝对 query 索引。

---

## 附录 B · 协议（所有 det 数字共用）

- 切片：深块 HB 12–15、去噪 9、8 步尾窗
- 风险集：query t 处仍存活的分支（部署条件）
- AUC **只在组内配对**再按配对数合并 —— 跨组合并会把 0.55 抬到 0.88，读的是初态难度
- 家族最大值用组内标签置换 + maxT 定价（2000 次）
- 新组织一律先对基线做**组内秩残差**再定价
- **禁用 top-4 硬 ID**（4/5 名精确平票 23.8%）；top-4 *质量*只用数值和，不受平票影响

---

## 附录 C · 已关掉的岔路（有人问再翻）

| 假设 | 结果 |
|---|---|
| 某个 expert 是失败开关 | 冻结的 2 条 prospective 信号全没复现（p=0.844 / p=1.0）；2,560 格最好 maxT p=0.771 |
| 瞬时统计量可免缓冲部署 | `ac_top4mass` 在 A 上三种读法**全部低于零分布中位**，current/d_win8 各 0/80 格 |
| top4mass 与变化率结合能互补 | 拟合 LOGO 24 格只有 7 格为正，中位 −0.028 / −0.014；t=30 六格五负 |
| state token 是独立通道 | 与 action 基线相关 0.83–0.92；被证伪 |
| 深层 churn 是前兆 | 逐 worker 符号翻转；churn 标记的是已陷入状态 |

---

## 附录 D · 产出文件

| 文件 | 内容 |
|---|---|
| `gate_compression.py` / `.csv` | Slide 7：44 位点的可见 vs 丢弃半边 |
| `moe_info.py` | Slide 6：名义/自由/有效维、PC 判出率 |
| `tokdisc.py` / `tokdisc_target1.csv` / `tokdisc_target2b.csv` | 逐 token 判出率（结局 + trap onset） |
| `top4mass.py` / `top4mass_results.csv` | 附录 C：瞬时量三种读法 |
| `combo.py` / `combo_results.csv` | 附录 C：组合的加性增量 |
