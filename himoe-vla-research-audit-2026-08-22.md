# HiMoE-VLA 路由研究：全面审计与综合分析（2026-08-22）

范围：2026-08 全月的实验、思路与结论（复现层 → 路由捕获层 → 机制分解层 → 8/21 九连实验），
基于三路独立全量提取 + 论文源文件 `example.tex` 定准 + 本日三个新判定实验
（`audit_seed_leak.py` / `audit_pathlen_control.py` / `audit_rules_history.py`，结果 JSON 已落盘）。
审计只修改了 memory 与本报告；未改动任何既有研究文档与 hub 数据。

---

## 0. 总评（先读这五行）

1. **主线结论站得住**：推理期路由 ≈ 各 token 输入的向量量化读出（state token = pose code，
   action token = 当前噪声 latent 投影），因果上不动成功率，可用性各线全部关闭——多任务、
   多 checkpoint、两个 benchmark 一致，且 8/21 九连实验在无人汇总的情况下独立收敛到同一图景。
2. **两个"routing 超越物理态"残留，今日一关一存**：t08 path-length 优势被判定为表示空间的
   度量伪影（**关闭**）；t08 软规则增量在 seed-disjoint 与 40 维历史增强基线下**原样存活**，
   但其正确定性只能是"有限容量基线的提取差距"，不是超越物理态的信息（香农上界，REPORT 自己论证过）。
3. **需要修正的是表述与归因，不是数据**：p=0.0386 张冠李戴、DOC1 未复制的 p=0.049 无更正横幅、
   "论文分数"三套并存、"+11.9 pt"把 union 的结果记在旗舰规则头上、CALVIN 结论坐在被取消资格的
   run 上——共 8 项 A 级修正，全部是文档层面。
4. **过程质量显著高于常规**：配对种子、bit 确定性门控、置换 max-statistic、位姿孪生、双留出、
   内建零对照、以及一份从未动用的冻结预注册——该方法学 battery 本身是可发表资产。
5. **下一步一句话**：修文档 → CALVIN 干净重采（坐标系结论是最强正面声明）→ 以"VQ 机制账户 +
   审计方法学"为主线成文；不再回头做任何 selector/pruning。

---

## 1. A 级：必须修正的表述/归因错误（全部文档层面，按优先级）

| # | 问题 | 证据 | 处置 |
|---|---|---|---|
| A1 | `himoe-vla-libero-alignment-2026-08-15.md` 把 **McNemar p=0.0386** 挂在"4 套件 × 500 局、差 15–19 pp"的句子上 | 该 p 值实为 8/03 的 **50 局 Goal 配对面板**（40/50→48/50，12 个不一致对） | 改归因或删 p 值；2000 局的真实配对 p 会小若干个数量级，现状反而弱化了自己的证据 |
| A2 | `himoe-vla-moe-routing-analysis-2026-08-07.md` 头条"整块消融 28%→10%，p=0.049"**未通过 8/10 复制**（14%，p=0.143），工作报告已写明"不应再引用"，但 DOC1 **没有更正横幅**（其他被推翻文档都有） | `abl2-*` 四臂重跑 | 加横幅："方向可信、50 局不足以定量、p=0.049 不可引用" |
| A3 | **三套"论文分数"并存**：`example.tex` 表（spatial **98.2**/object **99.4**/goal **98.6**/long **95.8**）；DOC3 与 corpus 记忆引用的"97.8/94.2/96.6"（实为本项目 checkpoint-right 复现值，被误标为"论文 Section 4.1"）；_pipeline 内部参照 92.4/95.4/98.0 | 本日 grep `example.tex:263` 定准 | 全项目统一：引用论文 → 只用 98.2/99.4/98.6/95.8；"复现到 ≤1 局"的对照对象是**复现参照值**，不是论文；paper-right 下 spatial 95.4 与论文 98.2 **不相容**的结论不变 |
| A4 | rules REPORT 结论 3"位姿配对后还剩 **11.9 pt**"——该数字属于 **top-20 并集**；旗舰规则 `L2:e5&L5:e8` 自己的孪生差是 **+0.030 [−0.004,+0.066]，CI 含零**；且并集孪生匹配率 23% vs 随机对照 83%，差异性匹配率本身可引入偏倚 | REPORT.md:112,114,200 | 结论 3 改写："并集层面配对后残差 +11.9 pt；旗舰规则单独未过孪生检验" |
| A5 | "无确定性失败规则"的论证：最纯规则支持度 **30 > 零假设 95 分位 29**（名义上在**相反方向**边缘显著），且 log 09 显示可评估连词 **19,077**/32,896（REPORT 未披露分母缩减） | REPORT.md:69-70,198；logs/09 | 改写为"最纯规则处在偶然边缘（30 vs 29），无高支持度纯规则；纯成功方向可达 49" |
| A6 | **CALVIN 坐标系结论**（joints R² 0.831 vs EEF 0.584）建立在被 hub 审计取消资格的 run 上：80/1000 截断探针、`episode_id` 全零、manifest 无记录、hub README 声称"无 CALVIN 采集路径" | hub 审计 §5-A-1；本日复核 `analyze_calvin_pose_code.py` **分割方法无问题**（leave-one-instruction-out，33 组） | 结论方法学成立、出处不合格：发表前必须干净重采（见 §7-②） |
| A7 | hub 审计文档自身矛盾："9/9 run hook_verified 480" vs "pin-on/off/smoke = 0"——**无 hook 自校验的恰是因果干预臂**；pin-base 是 bit 级重放，其"CI"不含独立抽样信息（配对检验无碍，表述要改） | 审计 §3 vs §5-D | 修正分母表述；pin 干预的行为学证据（87–90% 轨迹改变、20 万+ 替换）不受影响 |
| A8 | transplant 的 action-delta（0.11–0.18）**无磁盘工件**，n=16 各臂是极低功效 null 而非"无效应" | `runs/patch-probe-k11-s24` 只有 4 个 JSON | 维持禁引；如需此结论需带 per-episode npz 重跑 |

---

## 2. B 级：需要降级或加 scope 限定的结论

- **B1 t00/s24 步 11–12 "beats world state"（8/11-12）**：旧非确定管线（同种子 27/64 vs 23/64）、
  单初始态、4 种控制规格事后挑"最干净"、kNN 控制在 25 维/n=64 有欠拟合风险、承诺网格显示该窗口
  坐在 79–89% 已解决的结局上。**降级为 suggestive**；要保留就在新管线上预注册重跑（§7-④）。
- **B2 "routing 领先世界态 1–2 步" vs t08 "pose 步 8 领先、routing 步 13"**：两者可同真但**不得混引**。
  前者（DOC2 §9.1）另有未解释的同 z 异 p（+5.78/.001 vs +5.78/.006）、FWER 只在 (step×budget) 网格内、
  "复制"实为同 cell 重采（真正不同的两个 bundle 一胜一败且败者截断在步 ≤7）。引用时优先 t08 侧
  （512 局、max-stat 校正、家族 p=0.0005）。
- **B3 重叠分割的 ±SEM**：20 个随机 8/8 分割共享数据，有效自由度 < 19，transfer z ±0.31 与
  honest ±0.0034 都低估不确定度；16/20 的符号检验 p≈0.12。规则增量的表述强度按此校准
  （"稳健出现于绝大多数分割"而非显著性语言）。
- **B4 DOC1 §5.2 的操作点**是天花板实验失败后改到 released-left 的；单任务；不一致对 n=17/22。
- **B5 操作点三分裂**：released-left（8/7–12 全部结局类结论）、checkpoint-right（30 任务语料）、
  paper-right（对齐表 + t08 时代 + 新采集规范）。**永不同表**；8/12 前后三变量同变
  （任务、操作点、管线），跨时代比较必须显式注明。
- **B6 专家近平局的幅值数**（随机换 1 专家动作 4%、24% 平票）出自 n=1 合成观测 CPU bf16；
  平票**结构**已在真实数据复核（10,030/10,030 fp16 平票），幅值未复核（GPU/真实观测待做）。
- **B7 8/21 九连的 scope**：flow-lead 系三个实验 = 1 任务 1 场景 4 条相关 rollout；
  `expert-activation-future` 的成功 AUC 0.585（p≈0.06）为边界证据，"+0.117"必须带
  "跨 denoise 不稳、log loss 无改善、探索性"三连注。
- **B8 证据链缺口**（流程性）：52/60 根目录脚本 print-only、/tmp 中间件已删、fig/ 12 图无文档引用、
  §2 的 29.7%/48.1%（换全部 4 专家的 MoE 输出变化）与 `counterfactual_agg.json` 的 9–19%
  （随机重选路由的输出变化）是**不同实验**，极易混引。

---

## 3. 今日三个判定实验（新证据，脚本+JSON 已落盘）

### 3.1 (a) t08 path-length：**异常关闭** —— 是表示空间的度量伪影

`audit_pathlen_control.py`：把 proprio 经 LOSO RFF-ridge（R²=0.939）映射进同一 256 维路由空间，
在"纯姿态函数"的预测轨迹上量同样的路径长度：

| 轨迹表示 | path-len AUC | 回归掉夹爪开合后 |
|---|---|---|
| 真实路由（复现原结果） | 0.817 | 0.723 |
| **姿态预测路由（纯 pose 函数）** | **0.848** | **0.763** |
| 路由残差（真实−预测） | 0.623 | **0.495（p=0.91，零）** |
| 47 维 sim_state（原"物理态"对照） | 0.733 | 0.570 |
| **裸 8 维 proprio** | **0.800** | 0.689 |

三个事实合起来关掉这条线：纯姿态函数**反超**真实路由；路由的非姿态残差在控制后为纯零；
原对比里"物理态"低是因为 47 维稀释了手臂通道（裸 proprio 已 0.800）。
→ `routing-trajectory-shape-beats-world-state` 需要更正：优势属于"pose 嵌入的度量"，不属于路由信息。

### 3.2 (e) seed 泄漏：**排除**

`audit_seed_leak.py`：t08 上 32 个共享 flow seed 无结局主效应（方差低于零假设中位数，p=0.76）；
规则在 **seed-disjoint** 分割下照常迁移（top +0.156±0.017，19/20；挖出的仍是 `L2:e5&L5:e8`）。
共享 seed 设计对规则结论无泄漏（对"选择器/模板类"分析仍是系统性风险，8/21 `early-action-head`
已实测到 seed 模板效应——那条警告保留）。

### 3.3 (c) 历史增强基线：**规则存活**，但定性必须收口

`audit_rules_history.py`：先**逐位复现**了发表协议（142 维基线 log-loss 0.513 bit；
pool 增量 +0.0108±0.0034，16/20——与 REPORT/log09 完全一致，证明管线等价），再加 40 维
本体感受历史（滞后位姿、速度、累计路径、开合次数、驻留）：

    log-loss 0.513 → 0.513（历史特征对基线零贡献）
    pool 增量 +0.0108 → +0.0120 ± 0.0037（16/20）——原样存活

结合 3.2：t08 规则既不是姿态历史谓词也不是 seed 模板。但它的**正确定性**由 REPORT 自己的
香农论证限定：routing ⊆ f(观测)，H(Y|routing) ≥ H(Y|物理态) 恒成立，因此任何正增量在原理上
只能是**有限容量基线对状态的提取差距**——网络免费送出一个学到的非线性阈值特征
（VQ 胞元边界），HistGBT 在其容量预算内重构不出来。正确表述：
"路由为有限基线提供免费的学习特征，在最难任务上值 ~1 个 log-loss 百分点"，
而非"路由携带物理态之外的信息"。

---

## 4. 站得住的结论账本（当前最佳陈述，12 条）

| # | 陈述 | 强度/范围 |
|---|---|---|
| 1 | AS-MoE 按 `data_mask` 常数路由，层级顶层在单一动作空间内零方差；v1/v2 两份代码一致，非版本漂移 | 构造性事实，跨 30 任务/CALVIN 实测确认 |
| 2 | HB 动作 token 路由近均匀是 **HB-Reg 的设计目标达成**（论文 :795 自述），不是发现；state token 前块**锐利**（top-1 至 0.956） | 3 checkpoint 复制 |
| 3 | state token 路由 = 本体感受的非线性码：LIBERO t08 96%、5 任务 86–95%；**坐标系跟随动作空间**（CALVIN joints 0.831 > EEF 0.584，共线性对照通过） | 最强正面结论；CALVIN 侧待干净重采（A6） |
| 4 | action token 路由 = 当前噪声动作 latent 的投影：几何 rho 0.880，控制当前 latent 后与最终动作 −0.079 | 8/21 新证据；1 任务待复制 |
| 5 | 专家是近正交的真函数（输入依赖分量 4–7× 常数分量），**槽位身份跨全部 5 个 checkpoint 保持 32/32**；换 4 专家动 MoE 输出 31–50% | 跨 checkpoint 复制；"专家可互换"已被否证 |
| 6 | router 选幅值：前块选"写最多的"（r≈+0.45），末层选"写最少的"（−0.43）——增益控制，非语义分工 | t08 测得，与后块惰性四证一致 |
| 7 | 唯一的输入依赖开关：前块 state token 夹爪门（双峰、bit 级噪声免疫）；**因果上只改哪些局成功（φ 1.000→0.106），不改成功率**（−1.37 pp CI 含零，功效 94%@2pp） | 因果结论中最干净的一个；t05；t08 未测 |
| 8 | 结局在步 0 未被首步路由锁定（16/16 行翻转、MI p=0.92）；按**episode 长度的比例**（~88%）大部解决（79–89%）；"步 11"是分数不是索引 | 两个任务调和；分数说法是假设性调和 |
| 9 | 失败吸引子 = 手臂停泊（物理事实）；路由如实报告但 pose k-means 在所有符号动力学指标上**更强** | t08 完整、跨任务部分（stove 无衰减） |
| 10 | 全部 selector/pruning 关闭：路由/动作云几何/噪声中心度在诚实分割下均无收益，机会真实存在（O₃₂≈5 SD）但不可从候选云几何恢复 | 8/15 关闭 + 8/21 双留出再确认 |
| 11 | t08 软规则：诚实分割 +0.011~0.013 超越 {场景+47 维态+proprio+相位+动作块+**40 维历史**}，seed-disjoint 存活；定性=有限基线提取差距（§3.3）；**单任务**，其余 3 任务≈0 | 唯一存活的"超越"信号，带 A4/A5/B3 附注 |
| 12 | 复现层：paper-right 下 Goal/Object 与论文相容，**Spatial/Long 不相容**（95.4 vs 98.2、92.4 vs 95.8）；评测协议论文全缺（replan=10 由 ICLR 补充材料确认）；π₀ 基线行与 openpi README 位相同 | 4×500 局；训练步数假设已由作者自己放弃 |

---

## 5. 第一性原理综合：路由为什么"是"这个东西

**定理式陈述**：在（i）专家近正交（pairwise cos ≈ +0.03）、（ii）组合权重近平（top-4 质量 1.1×均匀，
HB-Reg 最小点）、（iii）主损失支配 router 梯度（aux/flow = 1–10%）三个条件下，top-4 选择
≈ h 在 32 个固定方向上投影幅值的 argmax 组 = **h 所在流形的向量量化胞元编号**。于是：

- 路由的信息量 = 该 token 输入流形的支配变量。state token 的 h 始于 `state_proj(proprio)`
  → pose code；action token 的 h 由当前噪声 latent 支配 → 去噪路径码。**两条通道一个机制**。
- VQ 胞元扰动对输出结构性鲁棒（正交基 + 平权重 + shared 分支兜底）→ 一切因果 null、
  4% 动作敏感度、pin 只重掷实现。
- 已验证的推论：binary top-4 解码优于连续概率（0.892 vs 0.831，码本性质）；跨任务可分性 2.80
  （不同 pose 分布）；CALVIN 输入更丰富则纯度 96→72%（量化器忠实于输入分布）；
  bf16 平票规制（量化边界天然简并）。
- 今日两个判定实验也落进框架：path-length"优势"= VQ 嵌入的度量集中于手臂（3.1）；
  规则增量 = VQ 边界作为免费特征的提取优势（3.3）。
- 前/末层的 +0.45/−0.43 幅值相关给出训练动力学解释：router 被主损失塑造成
  幅值/衰减选择器（前块放大、末层抑制），与"后块惰性"四项独立测量一致。

**边界**：该理论是对本 release（及其训练配方）的机制账户，不是 MoE 普遍定律——
可检验的外推预测是"锐化路由（低熵/强 z-loss/大 U）的 MoE-VLA 应当出现更语义的路由"，
这本身可以作为论文的 discussion 卖点。

---

## 6. 8/21 九连实验（"最近的实验"，首次成文收录）

12:46–21:38 在 `himoe-route-capture/analysis/` 连跑九个，全部自带 report.md+summary.json，
但 8/21 的 rollup 写于 12:06，**六个实验晚于它，至今无任何汇总收录**（本节即收录）：

1. `expert-activation-future`：终于测第三通道（真实 w_e·E_e(h)，对齐率 0.961）。同推理动作：
   expert-size +0.015、routed 方向 +0.060 但 **shared 方向 +0.092 更强 ⇒ 非专家特有**；
   episode 成功：step0 条件 AUC 0.585[0.521,0.649] 但跨 step 不稳、log loss 无改善。
2. `weighted-noise-evolution`：初始噪声经跨维线性映射对最终动作 AUC 0.797；
   发现旧分析拿 24 维 latent 当目标会高估（17 个死维保噪声）。
3. `unsupervised-moe-dynamics`：**账本第 4 条的出处**——action token 路由 = 当前 latent 投影。
4. `cumulative-moe-prefix`：route = phase backbone + noise response；作者自控显示
   backbone 控制 robot state 后残差 rho 仅 0.126 ⇒ 又是状态进度（pose）。
5. `route-noise-selector`：task-held-out 全 null；**发现路由中心度规则在提取固定 seed 模板**
   （16 状态全选同一 seed）；"中心=更好"的诊断上界也只 +0.020。
6. `early-action-head`：**发现 seed-template 混淆**（seed-only 模板 Spearman 0.919；
   双留出下 noise head 崩到 0.013）——所有共享 seed 的跨场景分析此后必须 seed-disjoint 验证。
7. `moe-value-signal`：双留出探针 router 0.381（无）、专家标量 0.587（p≈0.06 边界）；
   无监督规则 FWER 全灭；within 0.112/跨 0.010 复现 fork 结论。
8. `action-commitment-pilot`：route 读"计算完成度"真实（R² 0.968）但当前 latent 0.997、
   加 route −0.0005；medoid 到最后一轮才定。
9. `control-route-speed`：30 任务全语料描述性——相邻连续性比 0.866（p<0.0001）、
   lag~2-4 饱和、晚段更慢 0.905——与 pose 连续性一致。

**评语**：这一波把纪律升到了项目最高水平（双留出、FWER、诊断上界、零对照全在），
结论与主线完全一致；缺的只是收录与三个 flow-lead 实验的跨任务复制（B7）。

---

## 7. 现有信息能做什么 & 下一步

### 能做什么（按价值）

1. **一篇机制+方法学论文**（主线已齐）："What does MoE routing encode in a flow-matching VLA?"
   —— 正面主结果 = pose code + 坐标系跟随动作空间（账本 3）+ VQ 机制账户（§5）+
   良功效因果 null（账本 7）+ 完整负结果链（账本 10）+ 审计方法学 battery
   （配对种子/bit 确定性门控/置换 max-stat/位姿孪生/双留出/零对照/预注册模板）。
   负结果不是弱点，是这篇的骨架：它系统性地解释了"为什么 router 信号不是免费的内省通道"。
2. **数据与管线发布**：VLA_MUI_HUB 76 GB + libero30 语料 + bit 确定性捕获管线 + rules bundle
   （已有 SHA256 + 语料无关复现脚本）。发布前置：A7 文档修复 + `%02d` 对齐 + README 重写。
3. **不可发表清单**（维持）：transplant action-delta、pre-action protocol 的 28 cells、
   `pr-replan5-long` 部分数据、所有 released-left 时代未复核单次 64-draw 数字。

### 下一步（排序，含成本）

| 优先级 | 事项 | 成本 | 说明 |
|---|---|---|---|
| ① | A1–A8 文档修正批次 | 半天文案 | 全是改字，不动数据；A1/A2/A3 直接影响任何引用者 |
| ② | **CALVIN 干净重采** | ~1 GPU 日（MIG 2g） | 修 `serve_calvin_with_recorder.py` 的 episode_id，采足序列；坐标系结论从"出处有瑕"变发表级 |
| ③ | 规则的提取差距定界（可选） | ~1 天 CPU | 把基线模型类加强（小 MLP/更深 GBT/routing-维度对齐的特征预算）看 +0.012 何时被吸收——决定论文里"提取优势"的措辞强度 |
| ④ | t00/s24 预注册重跑（仅当想保留 early-readout 叙事） | ~3h MIG | 新管线 512 局、单一预注册控制规格（25 维+历史）、GBT 控制 |
| ⑤ | DOC8 预注册正式退役声明 | 10 分钟 | 操作点已废弃（released-left）；写一行退役注记，保留为方法学展品 |
| — | **不做**：第五种动作距离、更大 ranker、RAD 变体、重开 rollout selection、给 t08 规则再堆规则族 | — | 已三重关闭 |

### 意味着什么（三个受众）

- **对 HiMoE 论文**：性能层面——释出 checkpoint 在 paper-right 下 Goal/Object 复现、
  Spatial/Long 低 2.8–3.4 pp（且评测协议全靠 repo/补充材料）；机制层面——论文的层级分工
  叙事在部署规模上不成立（AS 常数、HB 近均匀是自己的正则目标、heatmap 来自 N=8,K=2 另一配置），
  但这不构成对论文的否证：**部署规模路由结构论文从未展示过，我们测的是新领域**；
  MoE 的真实贡献是容量/条件化（专家基 + 增益控制），不是可解释专门化。
- **对 VLA 可解释性领域**：router logits 是诱人但危险的解释对象——在本 policy 上它是
  机器人自带信息（proprio/当前 latent）的重编码，任何"routing 预测 X"必须对照
  {状态+历史+动作块} 且匹配模型类，否则是镜像。本项目的对照 battery 是可迁移的贡献。
- **对 MoE 科学**：专家槽位 = 预训练固定的正交函数基（微调只原位旋转 20–28%）、
  router = 幅值选择器（前放大后衰减）、小 U 下负载均衡损失协方差盲、bf16 平票规制——
  支持"MoE 作为自适应范数分配"而非"语义专家分工"的图景，且给出一个可检验的外推预测（§5 末）。

---

## 8. 审计方法与可复查性

- 三路提取：最新层（8/19 rules bundle、8/21 非复现报告、8/22 hub 审计、VLA_MUI_HUB、viz）、
  中旬层（8/07/09/15 四份 + route-capture 六份）、库存（60 脚本 → 问题映射、孤儿、最近活动）。
- 判定实验：`audit_seed_leak.{py,json}`、`audit_pathlen_control.{py,json}`、
  `audit_rules_history.{py,json}`（含对发表数字的逐位复现作为管线等价性证明）。
- 论文定准：`arXiv-2512.05693v2.tar.gz::example.tex` 流式 grep（未解包落盘）。
- 本报告不修改任何既有文档；A 级修正需用户批准后另行执行。
