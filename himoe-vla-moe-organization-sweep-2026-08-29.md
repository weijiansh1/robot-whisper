# MoE 路由的组织方式:646 种切法的暴力搜索(2026-08-29)

**动机**:此前所有路由检测结果都建立在一个薄片上 —— 去噪只取第 9 轮、11 个后缀 token 对 1–10 取平均、8 层按块平均。这既违反项目自己的铁律(「永远不要在后缀 token 上取平均」),也从未碰过 chunk 内那 10 轮去噪构成的轨迹。四个并行 agent 分头扫四条轴,共用同一套评测协议。

**一句话总账**:**换切法没用。646 种组织方式在对基线做残差之后全部塌掉,唯一活下来的那个要用标签。** 原因不是搜索不够,而是这个张量在「变化率」这个读法下只有一个自由度。

---

## 0. 语料与协议

| | 语料 A | 语料 B(独立复核) |
|---|---|---|
| 来源 | `runs/rolling-star-a100-long-t08-k16-20260828` | `VLA_MUI_HUB/.../KITCHEN_SCENE8_.../right-16x32` |
| 规模 | 352 分支(117 成功 / 235 失败) | 512 集(296 成功 / 216 失败) |
| 分组 | worker(4 个,即 4 个初态) | init_state_id(16 个) |
| 全员存活到 | t=31 | t=33 |

**评测**:目标为 BDDL `success`(不用手工失败分类);固定 query t 处风险集 = 仍存活的分支(部署条件);AUC **只在组内配对**再按配对数合并 —— 跨组合并会把 0.55 抬到 0.88,读的是初态难度不是路由;家族最大值用 200 次组内标签置换定价。

**基线**:后块 12–15、action token 1–10、去噪 9、与前一 chunk 的 Hellinger、8 步窗均值 → **A 0.795 / B 0.759**(t=30)。

**禁用**:top-4 硬 ID(第 4/5 名精确平票 23.8%,硬身份度量大半是 bf16 平局抖动)。

---

## 1. 四条轴的结果

### 1.1 去噪轴 —— 关闭

chunk 内 10 轮去噪的轨迹统计量(路程、净位移 d0→d9、净/路程效率、步长剖面、早晚轮对比),对 d9 基线做组内秩残差后:

| | 观测 | p |
|---|---|---|
| A | 0.688 | **0.055** |
| B | 0.631 | **0.070** |

两批都过不了自己的零分布。跨语料符号一致率 **114/240 = 48%(掷硬币)**。路程、净位移、效率三个量在两批间**符号全部翻转**(0.408↔0.674 / 0.432↔0.704 / 0.624↔0.566)。

去噪切片选哪一轮也几乎无所谓:10 个切片相差 <0.040(A)/ <0.019(B),偏好顺序两批相反(A 偏好 d9,B 偏好 d0)。

**附带裁决 — state token 跨去噪恒定性**:语料 B **逐位相同,max|p(d)−p(0)| = 0.000e+00,8 层全部、22883 行全部**。语料 A 不严格恒定(max 0.0234375 = 3/128)但功能上退化 —— L2 精确恒定,非恒定性随深度单调增长(L3 有 0.073 的元素不同 → L15 有 0.648),均值偏差 ~1e-5,state 与 action 在同一 Hellinger 度量下的运动比 **46–595 倍**(后块 46–60、前块 164–595),10 个切片下游 AUC 相差 <0.0008。A 的偏差**不是纯 fp16 量化**(14.2% 的元素超 1.5 ULP、6.1% 超 4.5 ULP),提示存在一条真实但极微弱的后缀双向注意力泄漏,B 没有。

### 1.2 层轴 —— 关闭

原始最好的格子 `chg_L14 | state token` 在 A 上 0.834、B 上 0.766。残差后:

| | 原始 → 残差 | 与基线相关 ρ | 家族 p |
|---|---|---|---|
| A | 0.834 → **0.658** | **0.852** | 0.294 |
| B | 0.766 → **0.626** | 0.698 | 0.100 |

残差层家族在 A 上 **p=0.0697 不过**;B 勉强过(p=0.0100)但最好的格子换了位置。88 个格子里**两批都过的:零个**。

**「state token 是独立通道」的假设被证伪**:A 语料里每一个 state token 层与 action token 基线的相关都在 **0.83–0.92**。

**降级保留的线索**:符号一致率总体 60.2%,但拆开看全部信息在 state token —— state 77.3% / action 50.0%(掷硬币),且 state 的一致率随 t 单调升高 64% → 73% → 82% → **91%**(t=30)。存在可复现的深度剖面且随 rollout 变锐,但小到过不了家族校正。**consistent effect that is not a significant effect**,仅作 suggestive。

### 1.3 token 轴 —— 关闭

逐 token 的熵组织最好只到 ~0.61。原始最好的是 `tok00|hell`(state token 的 Hellinger)A 0.815 / B 0.768,家族 p=0.005,但那本质上就是基线的 state token 版本,不是新东西。

### 1.4 统计量轴 —— 一个存活,但要用标签

| 子族 | A 残差 p | B 残差 p | 判决 |
|---|---|---|---|
| 瞬时(无历史) | **0.597** | 0.0149 | **否定** |
| 自参照历史 | **0.110** | 0.005 | 未证 |
| **参照他人成功** | **0.0100** | **0.0100** | **存活** |

**瞬时统计量彻底否定**:最好的 `I:tok_heterog` 残差后 A p=0.597。熵、top-1、top-4 质量、4/5 名差、到均匀的 KL/Bhattacharyya、Gini、参与率全部落在 0.46–0.55(A, t=30)。**没有免缓冲的部署方案。**

**记忆长度未证**:`hel_prev_mean_W12|front` 0.815→**0.655**(A)、0.777→**0.682**(B);`ewm_rate_a0.15` 0.800→**0.604**、0.776→**0.634**。超出 0.5 的部分掉 55–70%。点态两批 p≤0.045 同向,但家族层面 A p=0.110 不过,B 过了却是另一个统计量。**不能凭此把 W=12 提到 W=8 之上。**

**唯一存活**:`R:succ_minus_fail_ref_W8/W12`、`R:hel_to_succ_ref_W8` —— 拿**同组其他分支的成功**当参照。原始 0.914 A / 0.817 B;残差后 t=25/30 保持 **0.70–0.77**,点态与家族层面在两批都是 p=0.005–0.010、方向一致,符号一致率 **72.5%**(三族最高)。泄漏检验干净(置换后逐格均值 0.5065 / 0.5088)。

**但它买的是标签**:交叉拟合的有监督最近质心分数,其**无标签孪生版崩了(`R:hel_to_cohort_LOO_W8` 0.705 A / **0.431 B**)**,且在前块切片上输给朴素变化率。定性:**a better detector, not a better signal.**

---

## 2. 为什么暴力搜索必然失败:只有一个自由度

层轴 agent 的核心发现:**chunk 间的路由变化率是一个单一的共享因子,同时表现在全部 8 层和两个 token 家族上。**

- 后块 action 层与基线相关 **0.95–0.97**(A)
- A 语料里每个 state token 层与基线相关 **0.83–0.92**
- 最不冗余的是前块 action 层(ρ 0.45–0.73),但它们也最不预测 —— **独立性买不到东西**

646 种组织方式不是 646 个不同的量。这解释了为什么跨家族合并后只有 5/646 个格子双语料超基线(若彼此独立应有 ~25%),也解释了为什么残差一做全部塌掉。

---

## 3. 三条方法学结论(影响之后所有路由分析)

### 3.1 家族零分布若包含基线,p 值毫无意义

四个家族最初都**包含基线本身**,于是四家全部报回 p=0.0050 —— 那只是**基线过了零分布**,与被检验的新轴无关。去噪 agent 自行发现并补做残差族后,结论从「通过」翻转为干净的否定。

**规矩**:检验「某个新组织方式是否有增量」时,必须对基线做组内秩残差后再定价,并同时报告跨语料符号一致率 —— 后者往往比 p 值更能说明问题(去噪 48% = 掷硬币;层轴 60.2% 但全部集中在 state token)。

### 3.2 聚合顺序是没人声明的自由参数,值 0.109 AUC

同一句「后块 + action token + Hellinger」有四种合法实现,语料 B 上从 **0.642 到 0.751**,比整个 sweep 里任何层/token/跨层效应都大。而且排序两批语料间不稳(A 最好的在 B 最差)。

| 变体 | A | B |
|---|---|---|
| V1 先平均概率再算距离 | 0.808 | **0.642** |
| **V2 先算距离再平均**(推荐) | 0.799 | **0.751** |
| V3 先平均层、逐 token 算距离 | 0.802 | 0.744 |
| V4 先平均 token、逐层算距离 | 0.815 | 0.721 |

**规矩**:任何此类 AUC 不写明聚合顺序即不可复现。约定用 **V2(距离优先)**,理由不是它最好,而是只有它满足「块聚合 = 逐层值的均值」,逐层分解才自洽。

### 3.3 相位混杂,所有统计量共享(含基线)

t=30 时语料 A 的成功已走完约 **80%** 的 episode、失败才 **60%**(成功 T=33–46、失败 T=47–52)。每个统计量都部分在读任务进度,参照族读得最直接。**这不是某个方法的缺陷,是语料设计的性质**,与 08-28 审计指出的长度混杂同源。

---

## 4. 产物

`/tmp/moe_sweep_{token,denoise,layer,stat}.{csv,md}`、`/tmp/moe_sweep_*_permutation*.csv`、`/tmp/perm_null_*.npy`、`/home/jovyan/work/himoe-vla/analysis_stat_axis/`(代码 + 缓存切片)。

**注意 `/tmp` 会丢** —— 本轮 agent 工作已因进程退出丢失过一次,后改为边算边落盘才救回。要长期保留需迁出。

---

## 5. 收口后能引用的陈述

> 在两批独立语料上,MoE 路由的 chunk 间变化率能在 t≈30(物理判据触发之后)把失败与成功分开,组内配对 AUC **0.76–0.80**。对该基线做残差后,646 种替代组织方式中**没有任何无标签的组织方式**在两批语料上同时给出家族校正显著的增量。唯一存活的是有监督的「参照同组其他分支的成功」(残差后 0.70–0.77,两批 p=0.005–0.010),其无标签孪生版崩溃。
>
> **不成立**:提前量(t≤20 控制初态后为掷硬币)、周期性/振荡(RQA 确定性与最长对角线在 0.5 附近或以下)、瞬时突变(窗内 max 从不优于 mean)、免缓冲的瞬时统计量(残差后 A p=0.597)、chunk 内去噪轨迹(残差后两批均不过)、逐层通道(残差层家族两批都过的格子为零)。

---

## 6. 第二轮:暴力搜索聚合方式(13,494 种)

第一轮的框架错误:四个 agent 都在变**用哪一片**,把**怎么合起来**当固定。而 sweep 里单个效应最大的恰恰是聚合顺序。第二轮两个 agent 分头搜聚合本身。

### 6.1 那句话对应 144 种不同的算法

「后块、action token、去噪 9、Hellinger、8 步窗均值」在放置网格里对应 **144 个格子**,t=30 跨度 **A 0.322 / B 0.286** —— 从掷硬币到 0.825。**比 646 种切法测到的任何效应都大。**

`mean` 是**唯一次序无关的算子**(6 种归约次序下跨度 0.0003–0.0005);换 max/min/std/中位,归约次序本身值 **0.106**。这是第四个从未声明的自由参数(前三个见 §3.2)。

**强制报告规则**:必须逐轴声明**放置(距离之前/之后)、算子、范围**,外加**归约前的次序**。缺任何一项,数字不可复现。

### 6.2 距离函数怎么选无所谓,时间怎么聚合是全部

时间轴解释 **65–89%** 的网格方差,距离轴 **0.3–6%**(差 10–20 倍)。11 个 f-散度彼此秩相关 **0.989–1.000**。距离轴看似重要只因混进了「跟谁比」(前一 chunk / 自身滑动均值 / 标量变化)。**别用 EMD** —— 专家编号无度量意义,掉 0.03 AUC。

### 6.3 两个 suggestive 的幸存者

118/35100 个格子过联合 FWER-95(零分布 95% 给零、最大 3),收敛成两个效应,**都是 token 在距离之前 + 非均值算子** —— 第一轮从未进入的角落,因为它只会取平均:

| 组织方式 | 残差 AUC |
|---|---|
| t=25,11 个 token 概率**逐元素 min** 后算距离 | A 0.656–0.663 / B 0.656–0.693 |
| t=30,token 轴取 **std** | A 0.605–0.664 / B 0.661–0.674 |

第一个在 B 上原始 0.690–0.740,而基线在同一时刻是 0.525(掷硬币),与基线秩相关仅 0.19。**但去掉 state token 即死(B 0.690 → 0.515)**,与「单用 state token」相关 0.91/0.66 —— 仍是 §1.2 降级过的那个通道换门再现。**不是提前量。**

### 6.4 一个差点成立的发现,以及它怎么死的

**突发计数**(窗内超全局 q75 阈值的转换比例,W=12):残差 A 0.646 / B 0.634,两批点态 p=0.005,对十种距离和 q0.60–0.80 各阈值都稳定,双语料通过数 128 vs 零分布最大 76。

对五个窗均值残差 → 0.547/0.558;再加窗 std/max/min/中位与全历史指数加权 → **0.521/0.507,p = 0.67/0.86**,通过数 128 → 42 → **16(p=0.085)**。**它读的只是同一共享因子的记忆长度。**

### 6.5 最强的否定:相位

组内 episode 长度分辨结局的 AUC 是 **0.989(A)/ 0.999(B)**。对长度残差后:A 家族 p=0.005,**B p=1.000,联合通过数归零**;基线自己掉到 0.622(A)/ **0.506**(B)。

这是对**近乎标签**的量过度校正,不能读作「路由信号是假的」。可licensed 的诚实措辞:

> 在固定绝对 t 上,没有任何聚合方式携带的结局信息能在两批语料上同时与相对任务相位分离 —— 而这对基线本来就成立。

**并更正 §3.3**:相位混杂在语料 B 远比原文严重 —— 基线与 episode 长度的组内相关 **B −0.475 / A −0.079**。B 那边 0.759 的「独立复现」有相当部分在读长度。

### 6.6 两条 agent 主动交代的技术问题

- **会伪造结果的 bug,已修**:与基线完全同秩的格子做残差后留下 ~1e-14 浮点噪声,再排秩会把基线**复活成「残差」**。所有数为修后。
- **数值退化,已审计**:语料 B 的 state token 跨去噪逐位恒定,故对其取 std 会把 float32 舍入误差重归一化到满量程(中位 9.3e-10 vs action token 8.8e-4)—— 与被禁的 bf16 平局抖动同类。900 个格子涉及,**无一在 118 个幸存者中**,全删后结果逐位不变。

### 6.7 推荐

**保留现基线,并把它从默认提升为明文约定** —— 不是因为它赢(它没赢),而是因为:`mean` 是唯一次序无关的算子;只有它满足「块值 = 逐元素值的均值」;它位于网格中部,引用它不引入选择偏倚;两个控制都上之后没有东西在两批语料上胜过它。

记忆长度按自由参数如实报区间:**A 0.758–0.807 / B 0.751–0.781**。

**产物**:`agg_placement/`、`analysis_agg_axis/`(代码 + 报告 + CSV,已从易失的 /tmp 迁出)。

---

## 7. 第三轮:马尔可夫/MDP —— 路由链里没有 trap

前两轮都把张量压成**每 chunk 一个标量**再定阈值。马尔可夫模型是结构性的:「停住」= 吸收态、「打转」= 常返类、「trap」= 吸收集。状态空间标签无关地建(PCA20 + k-means,K∈{4,8,16,32,64},两种特征),转移矩阵与吸收概率交叉拟合。

### 7.1 干扰核查先做,这次是干净的

**状态不是长度码**:NMI(state, T) = 0.006–0.094,而 K 匹配的纯长度码天花板是 0.52–0.76 —— 只占 **1–14%**。NMI(state, at_cap) = 0.002–0.010(A)/ 0.06–0.09(B)。**不是 08-28 那次的重演。**

**但状态相当程度上是个时钟**:NMI(state, t) 占纯时钟天花板的 **28–37%(A)/ 45–66%(B)**。不过固定 t 的协议对此**构造性免疫** —— 把「纯时钟状态空间」(state = t 的 K 分位桶,零路由信息)灌进同一条管线,195 个格子里最高只有 **0.555(A)/ 0.558(B)**,因为固定 t 时所有存活分支占据同一个时钟状态。

### 7.2 没有 trap 集

- 瞬态图在 20 种配置中 **15 种是单一强连通分量**,唯一闭类是整个状态空间(K=64 时才裂出稀有态碎片)
- eps=0.05 下出现的「闭」单点每步仍漏 **3–12%** 质量
- **粘滞 ≠ 失败**:A/cell1280/K=64 的近闭单点 107 个访问者、失败率 **13%**(基线 67%);K=8 的另一个 88 个访问者、94%
- 吸收概率 h(s) 跨度(A 0.23–0.78、B 0.16–0.72,sd 0.03–0.13)**比基础失败率还窄**;Spearman(h, 到吸收的期望步数)在配置间**符号翻转**(−1.00 到 +0.80),故 h 不是长度码的重新包装

**结论:「停住 = 吸收态、打转 = 常返类」在路由空间里没有对应物。** 与 §6 相空间轮的判断一致(投影保不动点、不保环),但这次由马尔可夫机器直接判定。

### 7.3 驻留时间 —— 唯一存活的结构差异

平均驻留在 **20/20 配置**里都是最终失败更长(A/K=4: 4.94 vs 3.85;B/K=64: 1.70 vs 1.23),是找到的最可靠结构性差异。但 run 级 AUC 仅 0.50–0.57,分支级「t30 前最长驻留」0.50–0.64。汇编 §22 那条规则复现但召回可忽略:A/cell1280/K=32 在 19 条分支上触发、94.7% 失败(基线 66.8%);**B 在 K≥32 时从不触发**。

### 7.4 检测:低于掷硬币

**跨语料残差符号一致率 0.492,零分布均值 0.605,p = 0.965**,每个 t 都如此。无单个变体超过自身零分布(全部 p ≥ 0.124)。预先指定的 `absorb` 最差 —— **两批语料符号相反**(0.300 vs 零分布 0.763)。联合通过 11/390 格,期望 9.6。

**K 从不收敛**:`emp_pool_w8` 随 K 单调上升(A 0.650→0.796、B 0.699→0.805),同时平均驻留降到 1.2–1.5 —— *「它是在不再是状态机之后才变好的」*。吸收求解在任何 K/特征/t/语料下**都不优于无记忆的逐状态查表**;二阶链 37/72 胜(中位 +0.002)、半马尔可夫 55/120(中位 −0.004),都是掷硬币。

唯一双语料超基线的格子(半马尔可夫 K=64, t=30)扛住 5 次种子重抽:A 0.818±0.028 vs 0.799、B 0.772±0.016 vs 0.751(**+0.019 / +0.021**)。但同一格在 B 的 t=20 **输给**基线(0.555 vs 0.644)、t=25 为零。**一个时刻、两个百分点,从约 570 格里选出。**

### 7.5 判词

> **马尔可夫框架在标量阈值之上没有任何增量,因为路由链没有 trap —— 它是单一遍历通讯类,吸收概率的跨度比基础率还窄,剩下的只有「状态多久换一次」,而那就是标量基线的量化有损版本。**

驻留结果解释了**为什么**:驻留更长 = 状态切换更少 = 路由移动更小。**先离散化保留了「有没有越过 k-means 边界」,丢掉了幅度。**

### 7.6 两条应当成为标配的哨兵

**① 泄漏哨兵(免费的 0.95)**:把分支内 chunk 顺序**打乱**(非因果),同一条管线检测 AUC 变成 **0.93(A)/ 0.96(B)**。**任何碰到整条 episode 的管线白送约 0.95。** 此后所有路由分析都应先跑这一条。

**② 长度哨兵 —— 但必须按「决策时刻可知」读**:`T == cap` 的组内检测 AUC 在语料 B 是 0.9993、A 是 0.567。**这是事后 oracle,不是竞争对手** —— 决策时刻 t 并不知道 T。B 的最短 T=35,故 **t=30 时 512 条全部存活,长度在该时刻携带的信息恰为零**;A 在 t=30 可知的长度信息是 0.567,路由 0.799 明显超过它。拿事后长度否定只用 ≤t 因果特征的检测器是错误的推理。

**关于语料 B 的可用性 —— 更正**:先前写作「B 的失败由跑满上限定义,故 0.751 有相当部分不是路由信息」,这个推理不成立(见上:长度是事后量,t=30 时全员存活)。相位相关 ρ=−0.475 同样不是污染 —— 检测器本就该与「这条会拖很久」相关。

B 真正存在的问题是**另一条,与长度无关**:序贯规则的**打乱哨兵在 B 上反超真实顺序**(检出 0.694 vs 0.625,相位 0.17 vs 0.62)。即 B 上分数的判别力全部来自「整体水平」,chunk 的时间顺序不携带正信息。这一条独立成立,且是拒绝把 B 当时序结论复核语料的充分理由;但它不影响 B 上任何**非时序**结论。

另核实:语料 A 的 15,440 对组内分支**零前缀共享**,分支级交叉拟合干净。

**产物**:`analysis_mdp/`(代码 + 报告 + 状态标签 + 零分布)。

---

## 8. 第四轮:三个「推导出来的」设计,以及一个框架更正

### 8.0 框架更正 —— 两把尺子

§1–§7 对每个新方法都用了**方法选择**的尺子(对基线做残差、要求超过现有基线),然后把「没赢基线」报成「不成立」。这混淆了两个问题:

- **存在性**:MoE 里有没有信号 → 门槛是**超过平凡哨兵**
- **方法选择**:这个新算法比现有的好不好 → 门槛是**超过现有基线**

按存在性的尺子重算:**约 15,000 种组织方式几乎全部超过随机**(卡尔曼新息 0.69–0.71、CUSUM 0.574、逐层逐 token 多在 0.6–0.8)。**信号对提取方式高度稳健** —— 这是优点,不是「零胜」。

但「超过随机」不能只拿随机当参照,三样平凡的东西已经超过随机:纯时钟 **0.555**、跨组不配对 **0.88**、打乱 chunk 顺序 **0.61(A)/ 0.89–0.91(B)**。正确门槛是**超过平凡哨兵**。

### 8.1 卡尔曼新息(SSM)—— 否定,且我的推导被证伪

`y_k = clr(R_k)` 全部 88 个 (层,token) 站点 → 训练折 PCA → `z_{k+1}=Fz_k+Bu_k+w`,`u_k` = 执行的 10×7 动作块。EM 留一组拟合,前向滤波。模型健康(留出 NIS 34.8/31.8 vs 标定 32),**所以否定不是模型坏了**。

- **检测两批都输**:A 0.69–0.71 vs 0.799;B 0.54–0.70 vs 0.751
- **冗余检查通过**(|ρ| 0.44–0.71 < 0.9)—— 新息确实是新测量,只是更差
- **一个不用模型的静态马氏距离 `‖x_k‖` 打平整套装置**(0.70–0.72)
- 跨语料符号一致率 0.486/0.292/**0.625**;唯一符号一致的格子(滤波态范数)其**打乱孪生分数更高**
- 潜态 43–61% 是时钟

**我的预测被证伪,两批同号**:我推断「卡住 = 下大指令而场景不动 = 大新息」。实测相反 —— b(‖A‖) = **−0.29/−0.22**,「动作大+路由变化小」象限新息**最小**(−0.47/−0.49),「动作小+路由变化大」**最大**(+0.94/+1.01)。**含义:卡住时机器人并没有在下大指令,它不使劲了。** 这否掉了构造新息框架的前提。

**一条真结果**:动作输入使留出一步预测对数似然提高 **1.46/1.83 nats/chunk** —— 路由动力学确实由动作驱动 —— 但检测变化 ±0.07 无一致方向。**结构是真的,与结局无关。**

### 8.2 序贯检验(CUSUM/SPRT)—— 否定,且更正一条已发布说法

**更正**:先前从半成品数据按「同虚警检出最高」挑格子,报「相位从 0.58 提前到 0.52」。那是**选择偏倚**。正规做法(两条规则同一套留一组标定、配置跨语料迁移)后:

| | 序贯 | 固定阈值 |
|---|---|---|
| A 检出 @FA≈0.15 | 0.574 | 0.549 |
| B 检出 | **0.347** | **0.718** |
| A 相位中位 | 0.577 | 0.588 |
| B 相位中位 | 0.615 | 0.615 |

88 种配置的跨语料检出秩相关 **−0.046**。序贯只是**删掉了早期那个峰**(双峰 dip 0.88→0.04),不是增加早报。

**「50% 有信息故固定阈值在浪费」也被证伪 —— 信息不可累积**:t=25 单独一步携带窗口 0.635 中的 **0.607**;t≈21 之前所有步组内**在掷硬币或符号相反**;类别 LLR 分离 t≤25 只占 **22%(A)/2%(B)**。**8 步窗口已是正确的积分长度**;相位 0.60 是在等证据,不是在等阈值。

**哨兵给出反直觉事实**:累积和**天然与顺序无关**,打乱后序贯分数不变(A 0.477 vs 0.451–0.489);而**固定阈值被打乱明显打坏**(0.549→0.26–0.32)。**真正在读时序结构的是固定阈值。**

序贯唯一胜出处:**停滞类**(检出 0.90–0.94 vs 0.80,虚警更低,n=49)—— 唯一有持续签名的失败模式。

**另一条撤回**:「都不沾 100% 提前 13 步」实际只有 **n=1–3**(36 条里仅 5 条有 onset,中位 q42 在窗外),**不可引用**。

### 8.3 「盆地」的正确读法:停住,不是绕圈

用尺度不变的度量把「走得慢」和「走回去」分开:

| 度量 | AUC |
|---|---|
| 平均步长(慢) | 0.775 |
| 回转半径(展布窄 = 停住) | **0.750** |
| 约束比 = 展布÷(步长·√n) | **0.271** |
| 净位移÷路程 | 0.369 |

前两个真且相关(r=0.716);后两个反号,且失败(0.250)与成功(0.225)都贴着随机游走理论值 **0.29**。

**轨迹减速、展布变窄,但在自身尺度上仍是随机游走 —— 无向心约束、无回访结构。** 可视化应画**停住**(不动点),不应画**旋进**(极限环);PCA-2 里的螺旋是残余噪声的产物。

### 8.4 多数投票 —— 不成立,原因具体

六种组织方式秩标准化后平均 **0.775**、多数投票 **0.756**,均**低于最好的单个 0.823**。原因:六者两两秩相关**中位 −0.163**(范围 −0.432 至 +0.884)—— 熵与 top-1 类的 AUC 是 **0.43(反号)**,把异向项平均进来只会稀释。多数投票要求各票**同向且弱相关**,这里两个条件都不满足。

### 8.5 意外收获:`st_deep` 高于基线

单个组织方式在 t=30 的组内 AUC:**state token 后层 0.823** > action 后层(基线)0.804 > state 前层 0.792 > action 前层 0.622。

这与 §1.2 的 `chg_L14|state` 同向。当时因残差后塌、CI 含零而降级丢弃 —— 按**存在性**的尺子它是这批最好的单个,且与基线只是中度相关。**这是用错尺子丢掉的一个结果。**

### 8.6 失败类型分类:去掉固定控制步之后

在**整条轨迹 + 自身相对相位 5 段**(长度构造性不进入)上分类失败类型,留一 worker:

| 对比 | n | 长度哨兵 | state | action | 两者 |
|---|---|---|---|---|---|
| **停滞 vs loop** | 49/150 | 0.437 | **0.994** | 0.993 | 0.993 |
| 停滞 vs 其它 | 49/36 | 0.451 | 0.904 | 0.890 | 0.904 |
| loop vs 其它 | 150/36 | 0.488 | 0.562 | **0.649** | 0.608 |

长度哨兵全在 0.44–0.49;两个 worker 各自都有两类(w0 19/33、w2 28/35)。

**逐相位均值解释了机制**——前两段四类完全重合,分离全在 p4–p5:

```
段:      p1     p2     p3     p4     p5
停滞 st: 0.064  0.070  0.053  0.017  0.016
loop st: 0.061  0.068  0.063  0.059  0.061
成功 st: 0.062  0.076  0.076  0.076  0.062
```

**两通道冗余而非互补**(st 与 ac 同步下降),符合 VQ 账户:两条通道读同一份观测。

**限制**:相位归一化用到整条 episode 的总长度,故这是「已知失败后离线判类型」,**不能直接变成在线检测器**。0.994 的置换定价、语料 B 复核、逐组拆分正在进行。

---

## 10. 第五轮:在线检测(逐步放出前缀)

**归档在 `analysis_online_2026-08-29/`** —— 该目录自带 `README.md`(叙述)、
`FINDINGS.md`(可引用结论)、`CORRECTIONS.md`(本轮撤回的 8 条)、7 张 CSV、
可重跑脚本与缓存数据。以下只记与本文件其余部分的接口。

**主结果**:MoE 路由跨 chunk 变化率能在**全员存活的等机会窗口 t<35**(语料 B)内在线判定
成败,**6–8% 误报换 73.6%–75.9% 检出**,停滞 ~92% / other ~50% / loop ~46–56%。
只用 t 之前的路由,不用总长度。

**对本文件既有结论的三处影响**:

1. **§3.2 / §6.1 的「未声明自由参数」清单新增两项,且都比原有的大** ——
   **判定窗口上界值 25 个百分点**(同一配置 t<35 → t<52,检出 73.6% → 98.2%,
   而失败/成功触发机会比从 1.00 → 1.39);**滑窗宽度值 23 个百分点**(第 30 步 W=8 → W=2)。
   相比之下通道、距离、切法全部 ≤2.5 点。**协议参数比信号选择重要一个数量级。**

2. **§6.2「距离函数怎么选无所谓」的适用范围扩大** —— 原文只证 11 个 f-散度可互换。
   加权 Jaccard(Ruzicka)**不是 f-散度**,与 hellinger 秩相关 0.996,照样落进同一堆。
   所以这是**张量的性质,不是 f-散度族的性质**。唯二不同的是基于 top-4 的两个度量,
   而其中 `top4_mass` 对现象没有响应(自身塌陷比 0.998)。

3. **§2「只有一个自由度」推广到 token 轴** —— 11 个后缀 token 每个单独都给 73.6%–77.8%,
   所有跨 token 对比量塌到 16%–51%,11 维监督模型比 token0 单独 ±0.01。相关矩阵呈带状
   (相邻 0.93–0.95,衰减到 0.75),有效维度 1–2。**这条带状结构是 VQ 账户最直接的一次确认。**

**新的机制解释**(补 §2 的「为什么暴力搜索必然失败」):

> **滑动窗口是同质化器。** `top4_mass` 在逐 chunk 层面与 hellinger 秩相关 0.584,
> 取 3 步窗均值后跨通道升到 0.869–0.915。**能活到窗口之后的差异已经不是差异了。**
> 646 种切法与 13,494 种聚合全部塌掉,正因为它们都在窗口之后比较。

**对 `moe-churn-zero-lead-time` 的加强**:第 28 步之前单 chunk 判对率 55.9%–66.0%
(基线 57.8%),第 29 步跳到 78.3%。610 个放大器格子(自参照 z 分 / CUSUM / 差分窗 /
EWMA / 窗口前非线性跨通道)**无一胜过朴素窗均值**,且输的幅度正比于各自积分历史的长度 ——
**没有提前量,就没有可累积的前兆**,这是零提前量的一个可证伪推论,现已证实。
