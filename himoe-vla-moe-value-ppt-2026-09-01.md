# Robot-Whisper 汇报 PPT · 逐页写法

> 2026-09-01。骨架取自已发布的 blog(cckfdu.com/demo/robot-whisper),
> 数字全部来自本轮实测,协议统一为**组内配对 AUC + 置换检验**(跨组池化一律不用)。
>
> **结构**:正片 14 页(讲故事)+ 防御 8 页(经得起追问)+ 3 页撤回与限制。
> 正片按 blog 走,防御页只在被问到时翻出来 —— 但**必须提前做好**,因为正片里有三处
> 说法会被一个三行的基线打穿(见 §附录 D)。

---

# 第一部分 · 正片(14 页)

## P1 · 封面

**标题**:Neurons see the state the tokens don't —— 具身智能的内生异常警报
**副标**:VLA · LIBERO-10 · 864 rollouts · 四个事先冻结的常数
**底部**:向内探寻,向外进化 · github.com/Cck123123/Robot-Whisper

**讲稿(15 秒)**:今天讲一件事 —— 机器人卡住的时候,它自己不知道,动作也看不出来,
但模型内部的神经元活动里有痕迹。我们把它做成了一个警报,并且当场救回了一条注定失败的轨迹。

---

## P2 · 起点:一件旧事(NAD)

**内容**:
- 推理大模型上做过的探针 NAD:在多条候选答案之间,**不读一个字**,只凭神经元活动就能挑出更可能对的那条
- 把握与犹豫没有写进 token,但内部状态里有
- (引 Chen et al. 2025, ICML 2026 Spotlight)

**一句话过渡**:同一个思想换到具身场景 —— **VLA 大模型的内部,会不会也有类似的信号?**

**讲稿**:这一页是动机的合法性来源。不要展开 NAD 的细节,只要建立"内部状态比输出多知道一些"这个先验。

---

## P3 · 现象:两条只差一条噪声的轨迹

**视觉**:blog 的 Fig.1 两段视频并排(可用截图代替)

| | 结果 | 步数 |
|---|---|---|
| 左 | ✓ 两只壶先后上炉 | 38 步 · 18.6 s |
| 右 | ✗ 悬在第二只壶上方 | 52 步 · 26.0 s 超时 |

**同一个模拟器状态,唯一的区别是扩散头的一条采样噪声。**

**讲稿**:让听众自己看两段视频,然后说 —— 分开它们的那件事,从未出现在任何 token 里。
这是全场唯一一次"卖悬念",后面全部用数字。

---

## P4 · 物理世界的 trap 长什么样

**视觉**:blog Fig.2(累计 |Δ距离| 曲线,32 条)

- 固定同一初始状态,只变采样噪声,跑 32 条:14 成功 / 18 超时
- 前半段蓝橙同路:放好第一只壶(≈ +13 cm),然后一起歇脚
- **分岔在第 30 步上下**:蓝色重新起跳冲到终点;橙色**再也没涨过一厘米**

**讲稿**:强调纵轴是"累计变化量"不是"到目标距离" —— 因为有的 trap 就停在目标旁边。
「世界还在被改变,曲线就还在涨」。

---

## P5 · 为什么 token / 动作侧难办

三张卡:

| | |
|---|---|
| **01 动作流照常输出** | 每个卡住的步,模型照样吐出幅值正常的 10 帧动作 |
| **02 判决在终点** | 成功要等布尔判定翻真;而"长"两解 —— 慢成功和快失败读起来一样 |
| **→ 得去别处看** | 悬停也好、打转也好,动作序列上读不出分别 |

> ⚠️ **这一页是全场最脆的地方**,见 P17 与附录 D。
> 必须改口径:不能说"动作侧看不出来",要说"**动作侧看得出来,但它读的是任务阶段,不是卡住**"。

---

## P6 · 神经元:pattern 与它的动力学

**视觉**:blog Fig.3(浅层 L4 / 深层 L14 的热力图对照,金线 = t30)

- 每个控制步,模型点亮一小撮神经元(4 个深层 × 11 个动作 token)—— 这份名单叫 **pattern**
- 我们先试了读**身份**(谁被点亮):**没用** —— 身份是场景指纹,换个初始状态整套重排
- 有意义的是它的**动力学**:顺利时 pattern 随世界更迭;受困时更迭变慢

**金句**:信号不在内容里,在流动里。

**讲稿**:这一页要把"我们试错过"讲出来 —— 身份没用这件事本身是结果,能挡掉一半提问。

> ⚠️ blog 原文写"the same neurons fire again and again, and the thinking freezes"。
> **实测:连续 10 步始终点亮的专家只有 0.27 / 4,仍有 3.7 个在轮换。**
> 改口径:不是「冻结」,是「**换手速度降三分之一**」(d̄ 0.72 → 0.63)。

---

## P7 · 把凝固做成警报:三步

**视觉**:blog 的三步框 + 公式

| 步 | 内容 |
|---|---|
| **dₛ** | 相邻两步点亮集合的重叠距离 `1 − |∩|/|∪|`,平均成一个数 —— 路径的瞬时速度 |
| **r(t)** | 除以**自己开头 8 步**:每条 rollout 只回答"我比刚出发时慢了多少" |
| **θ·K** | 连续 K=3 步 r(t) < 0.95 → 报警。四个常数在救援实验**之前**冻结 |

$$r(t)=\frac{W^{-1}\sum_{s=t-W+1}^{t}d_s}{W_0^{-1}\sum_{s=1}^{W_0}d_s},\quad W=4,\ W_0=8$$

**为什么要除以自己**(这是可以现场丢出的硬数据):
> 变化水位 d̄ 的方差 **66.8% 由场景决定**(η²=0.668),自归一扣掉的正是这 67%。

**讲稿**:把 dₛ 翻译成人话 —— 正常干活每步换掉 2.2–2.5 个专家,卡住之后多留 0.4 个。

---

## P8 · 相空间:三种命运

**视觉**:blog Fig.4(动态相图;PPT 用静帧 + 箭头标注)

| 区域 | 对应 | 位置 |
|---|---|---|
| **巡航** | 正常干活 | r ≈ 1.0,轨迹不重复地游走 |
| **不动点** | 停滞类 | r ≈ 0.45,塌成一点 |
| **极限环** | 打转类 | 一直在动,终态混进巡航区 —— **最难分的一族** |

**后期阻尼比实测**:停滞 ζ = 0.83(29% 完全过阻尼)vs 打转 ζ = 0.31,同场景内 AUC 0.651。

**讲稿**:这一页把"失败"细分成两种物种,并且给出了定量证据。极限环那一族是漏报的来源,
主动说出来比被问出来好。

---

## P9 · 结果:抓住八成失败

**大数**:

| | |
|---|---|
| **81.2%** | 在线判对 · 864 条 rollout(跨任务、跨初始状态) |
| **+19.7 pts** | 高出多数类基线(61.5%) |
| **80% / 17%** | 失败召回 / 误报 |

**报警落点**:q18–q36,中位 **q28** —— 距超时还有一半路程。

**讲稿**:强调"警示器不看画面、不看动作、不看任务标签,只看 pattern"。

---

## P10 · 误报是什么

**视觉**:blog Fig.6 判决矩阵 + Fig.8 慢成功

- 352 条留档:失败 235 → 抓住 **184**、漏 51;成功 117 → 放行 98、误伤 **19**
- 被误报的成功中位 **41 步**,安静的成功 **37 步** —— 它们确实更慢
- **警报读作「正在下坠」,不是「必然坠毁」**

**讲稿**:这一页把误报讲成语义问题而非性能问题,顺便为下一幕的干预实验定调。

---

## P11 · 机制:环锁死(全场智力密度最高的一页)

**视觉**:

```
        ┌──────────┐
   ┌───→│   模型   │───┐  输出幅值正常的动作
   │    └──────────┘   ↓
┌──┴───┐          ┌──────────┐
│ 观测 │←─────────│   世界   │
└──────┘          └──────────┘
   ↑                    │
   └── 任务状态不再改变 ─┘
```

**关键**:没有任何一个零件坏掉 —— 模型正确响应了输入,物理也没错,是**复合映射有一个不是目标的不动点**。

**为什么模型抓不到**(policy_input.npz 的实证):

```
   image (224,224,3) · wrist_image (224,224,3) · state (8,)
   ─────────────────────────────────────────────────────
   没有一个维度是时间。历史不在输入里,是结构事实。
```

**三层障碍**:① 输入无历史(π0/OpenVLA 类)② 演示数据里没有失败(所有模仿学习)③ 卡住时历史窗口内全是同一帧。

> ⚠️ blog 原文的因果链写作「Stuck → observations unchanged → inputs unchanged」。
> **实测:sim_state 47 维中 19 维照常变化,末端位移保留 99%。**
> 改口径:不是「世界停了」,是「**任务相关分量停止更新**」。

---

## P12 · 失效在语义层,不在动力学层(本轮最重要的新发现)

**两层**:

| 层 | 内容 | 能否从数据学 |
|---|---|---|
| 动力学 | `s' = f(s,a)` 动作有没有产生预期的物理效果 | 能,无需目标 |
| **语义** | `Δφ(s)` 这个效果有没有让任务前进 | **不能,需目标监督** |

**实测(失败 rollout,卡住前 → 后)**:

| 量 | 变化 |
|---|---|
| 指令幅值 | −5.4% |
| 末端路程 | **+10%** |
| 末端净位移 | **+7%** |
| 回转半径 | **+17%** |
| **执行效率(位移/指令)** | **+4.8%** |
| **任务进展** | **−100%** |

> **手臂照常移动 3.3 cm/chunk,效率甚至略高。净/路程 0.52,远高于随机游走 0.29 ——
> 它不是原地打转,是大跨度、有方向地做无用功。**

**推论**:所有只学 `p(s'|s,a)` 的模型(世界模型 / 卡尔曼 / 逆动力学 / 动作条件视频预测)
在这类 trap 里**预测得非常准**,因此新息 ≈ 0。
本项目的卡尔曼实测:AUC 0.69–0.71,输给基线 0.799;预设的「卡住=大新息」被证伪。

**讲稿**:这一页是给懂行的人看的。它同时解释了 ①为什么外部预测式方法必然失败
②为什么我们最后用了一个不建模的量。

---

## P13 · 急救!!

**视觉**:blog Fig.10(报警 → 分叉 → 救回)

**做法**:q32 报警 → 冻结状态 → 只加一点干扰(大幅度改换采样噪声)→ 继续。其余一切不动。

| | |
|---|---|
| 救回 | **3 / 8** |
| 步数 | 52 步失败 → **8 步完成** |
| 时长 | 26.0 s → **3.9 s** |

**边界(同页列出,不要藏)**:
- 另一初始状态 **0/8**
- 随机时刻分叉同样 **3/8** —— 报警的价值在「知道卡住了」,不在「这一刻特殊」
- 按路由变化幅度挑候选选不出赢家(n=1)

**讲稿**:主动把三条边界念出来。这是全场最容易被质疑的一页,先手交代能把质疑变成信任。

---

## P14 · 正片收尾

**三句话**:
1. 具身失败在**动作层不可见、在语义层发生、在神经元的更新速率上留下痕迹**
2. 四个事先冻结的常数 → 864 条 rollout 判对 81.2%,一套阈值跨任务不重调
3. 报警之后**还来得及**:26.0 s 的失败变成 3.9 s 的成功

---

# 第二部分 · 防御页(8 页,备用)

> 这些页不进正片,但**必须做好**。它们对应审稿人/听众一定会问的问题。

## D1 · 「用动作幅值不就行了?」

场景×快照内分层 AUC:

| 检测器 | t=20 | t=25 | **t=30** | t=35 |
|---|---|---|---|---|
| 动作幅值 | 0.638 | 0.622 | **0.832** | 0.748 |
| MoE 路由 | 0.478 | 0.685 | 0.801 | 0.742 |
| 末端位移 | 0.564 | 0.656 | 0.795 | 0.763 |
| 仿真状态 | **0.688** | **0.761** | 0.761 | 0.736 |
| 模型输入状态 | 0.493 | 0.572 | 0.740 | 0.775 |

**诚实回答:是的,动作幅值在 t=30 上赢了。**接 D2。

---

## D2 · 但它赢在哪里 —— 进度计,不是卡住检测器

同一条 rollout 内部的响应:

| 量 | 失败组(卡住前→后) | 成功组(开头→末段) | **对 trap 的效应量 d** |
|---|---|---|---|
| **MoE 路由** | **−12.6%** | −1.9% | **0.67** |
| 动作幅值 | −5.5% | **+24.1%** | **0.03** |
| 模型输入状态 | −67.2% | −26.1% | **3.30** |
| 末端位移 | −1.7% | +27.6% | −0.12 |

> **动作幅值的判别力八成来自「成功组末段猛干」(+24%),不是「失败组卡住」(−5.5%)。
> 对 trap 事件本身的效应量只有 0.03 个标准差。**

---

## D3 · 时效:MoE 是最晚的

等误报率 ≈15%、等机会窗口 t<37:

| 检测器 | 检出率 | 报警中位 |
|---|---|---|
| 动作幅值 | **89.8%** | q29 |
| 仿真状态 | 85.5% | **q25** |
| 末端位移 | 76.2% | q30 |
| **MoE 路由** | **56.2%** | **q31** |

**「更早/更强」这两句话不能说。**

---

## D4 · 那 MoE 赢在哪 —— 跨任务阈值迁移(唯一明确胜出)

在 object/t00 标定到 15% 误报,阈值**原样**搬到其余 22 个任务:

| 检测器 | 误报飘移 | 搬家后误报 | **搬家后检出** | 判读 |
|---|---|---|---|---|
| **MoE 路由** | **8 点** | 0% | **69%** | **可用** |
| 模型输入状态 | 24 点 | 10% | 100% | 可用,误报翻倍 |
| 动作幅值 | **100 点** | 2% | 100% | 一个任务全员误报 |
| 动作块变化 | 10 点 | 0% | **7%** | **伪稳定 —— 等于关了** |

**动作幅值 100% 误报的成因**:标定任务是单阶段抓取,goal/t03 是「开抽屉+放碗」两阶段任务,
开头 8 步是慢速接近 → 自归一基线偏低 → 后半程全部越线。
**任务内最强 + 跨任务最脆,同时成立。**

---

## D5 · MoE 稳的原因:三重不变性

| 层 | 内容 | 谁有 |
|---|---|---|
| ① 归一化不变 | 集合重叠 ∈ [0,1],不是"多少米/多少弧度" | **仅 MoE** |
| ② 重编号不变 | \|∩\|/\|∪\| 不认专家编号 | **仅 MoE** |
| ③ 自身基线归一 | 除以自己开头 8 步(扣掉 67% 的场景方差) | 所有检测器共有 |

**结论口径**:MoE 换来的不是判别力,是**"一次标定,到处能用"**。
这个价值在单任务 benchmark 上完全看不见,只在多任务/多本体部署时兑现。

---

## D6 · 残差化:路由在外部量之外还剩什么

| | AUC | p |
|---|---|---|
| 路由(原始) | 0.801 | <0.0001 |
| **路由 ⟂ 模型输入状态** | **0.780** | <0.0001 |
| **路由 ⟂ 输入+动作+仿真状态** | **0.619** | 0.046 |
| 模型输入状态 ⟂ 路由 | 0.688 | <0.0001 |

相关:路由 vs 输入状态 ρ=+0.208(**几乎不共线**)、vs 动作幅值 0.562、vs 仿真状态 0.667。

**读法**:路由不是输入的线性影子,但它与「世界在不在动」共线较多;
扣掉全部外部量后还剩 0.619,刚过显著线。

---

## D7 · 「你们试过别的切法吗?」—— 全部关闭

| 方向 | 规模 | 结果 |
|---|---|---|
| 组织方式暴力搜索 | 646 种 | 残差后全塌 |
| 聚合方式暴力搜索 | 13,494 种 | 时间聚合值 0.109 AUC,信号选择 ≤2.5 点 |
| 马尔可夫 / MDP | 390 格 | 路由链里没有 trap 集 |
| 卡尔曼 / SSM | — | 0.69–0.71,输给基线 |
| AR(2) 阻尼比 ζ | — | 场景内 0.40–0.49 |
| 14 项峰值特征 | — | 全零;唯一 p<0.05 者复现符号翻转 |
| 9 项峰差比(TPR 族) | — | 全零 |
| token 之间的分歧 | — | 0.472–0.543;效率靶子 ρ=+0.001 |

**峰值族为什么必然失败**:连续摆幅比 D2/D1 中位 **1.053**(不衰减),与白噪声分布重合(AUC 0.480)。
**没有瞬态 → 峰值法的前提不存在。**

**核心解释**:chunk 间变化率是**单一共享因子** —— 后块层与基线相关 0.95–0.97,
state token 层 0.83–0.92。**这个张量在"变化率"读法下只有一个自由度。**

---

## D8 · 零提前量,而且有因果解释

窗口扫描(long,场景×快照内分层):

| 窗口 | 占失败段 | AUC | p |
|---|---|---|---|
| 前 16 步 | 32% | 0.537 | 0.48 |
| 前 20 步 | 40% | 0.519 | 0.73 |
| 前 24 步 | 48% | 0.461 | 0.46 |
| **前 30 步** | **60%** | **0.236** | **<0.0001** |

信号在 q24 → q30 之间出现,而 trap onset 正在 q30。

```
   世界:可改变 ─────╳ 不可改变
   观测:在变   ─────╳ 不变        三者同时翻转
   路由:在换   ─────╳ 更新减慢

   路由是观测的函数 → 不可能比世界更早知道。因果顺序,不是灵敏度问题。
```

**模型对自己卡住毫无觉察**:

| 量 | 前 14 步 | 卡死后 | 变化 |
|---|---|---|---|
| 路由熵(信心) | 3.4539 | 3.4524 | **−0.04%** |
| token 之间的分歧 | 0.2556 | 0.2541 | −0.5% |
| top-4 概率质量 | 0.1548 | 0.1570 | +1.4% |

---

# 第三部分 · 限制与撤回(3 页)

## L1 · 限制清单(建议放正片末尾,不要只放附录)

1. **跨任务迁移只有一个标定源**(object/t00);留一交叉尚未做
2. **失败样本稀少**:HUB 成功率 92–98%,仅 6 个任务有 ≥3 条失败
3. **跨本体无数据**:同一台 Franka、同一 checkpoint;"换机器人会崩"是量纲推论
4. **相位混杂**:固定 t 比较时成功组已走完 ~80%、失败组 ~60%,所有统计量都部分在读进度
5. **重放不可逐位复现**:模拟器快照不完整,干预实验引用存档首轮
6. **单任务场景下 MoE 无优势**:policy_state 更强(d=3.30 vs 0.67)也更简单

## L2 · 本轮撤回

| 原结论 | 撤回原因 |
|---|---|
| 早期路由熵可预测解题效率(ρ=−0.216, p<0.0001) | **窗口敏感**:同场景 W=6→−0.676、W=8→−0.312、W=10→**+0.277**、W=14→−0.439。符号是窗口的函数 = 噪声 |
| pattern 在 trap 中「冻结」 | 连续 10 步始终点亮的专家 **0.27/4**,仍有 3.7 个轮换 |
| 「卡住 → 观测不变」 | sim_state 47 维中 **19 维照常变化**,末端位移保留 99% |
| 「动作侧看不出来」 | 动作幅值 t=30 AUC **0.832** |
| 阻尼比 ζ 是场景指纹 | 用原始距离重算后场景只解释 12.6%;真正的场景指纹是水位 d̄(**66.8%**) |

## L3 · 复现信息

- **语料**:`runs/rolling-star-a100-long-t08-k16-20260828/formal`(352 条 = 4 初始状态 × 6 快照 × 16 分支)
  `corpus/libero30-right-v1`(3 套件 × 10 任务 × 50 条;**只变初始状态,流噪声固定**)
- **特征**:HB 12–15 × 去噪步 9 × 11 后缀 token × top-4 ID;`1 − |∩|/|∪|`,**先算距离再平均**(V2 口径)
- **检测器**:`r(t) = mean(近 W=4) / mean(自身前 W₀=8)`;连续 `K=3` 步 `< θ` 触发
- **统计**:组内配对 AUC 按配对数合并;置换检验 1500–5000 次;**跨组池化一律不用**
  (池化会把 0.586 抬到 0.86,读的是场景难度)

---

# 附录 D · blog 里三处会被打穿的说法(**上台前必须改口径**)

| blog 原文 | 实测 | 建议改法 |
|---|---|---|
| "The action stream cannot tell working from spinning" | 动作幅值 t=30 AUC **0.832** | 「动作侧**能**分,但它读的是任务阶段(成功组末段 +24%),对 trap 事件的效应量只有 0.03」 |
| "Stuck → observations unchanged → inputs unchanged" | sim_state **19/47 维照常变化**,末端位移保留 99% | 「**任务相关分量**停止更新」——世界没停,是任务停了 |
| "the same neurons fire again and again, thinking freezes" | 持久专家 **0.27/4** | 「**换手速度降三分之一**」(d̄ 0.72 → 0.63) |

**如果不改**:任何一个懂 robotics 的听众用一个三行的动作幅值基线就能把前两条打穿,
而且会连带质疑整个工作的严谨性。**主动改口径 + 把对撞表放进防御页,是把弱点变成强项的唯一方式。**

---

# 时间分配建议(20 分钟版)

| 段落 | 页 | 时间 |
|---|---|---|
| 起点与现象 | P1–P3 | 3 min |
| 物理 trap 与 token 的局限 | P4–P5 | 3 min |
| 神经元与警报 | P6–P8 | 5 min |
| 结果 | P9–P10 | 3 min |
| **机制(环锁死 + 语义层)** | P11–P12 | **4 min** ← 智力密度最高,不要压缩 |
| 急救 + 收尾 | P13–P14 | 2 min |
| 防御页 | D1–D8 | Q&A 按需 |
