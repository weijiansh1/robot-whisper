# 交互证据如何变成动作修订:白盒轮次 1 的设计与 stage 0(2026-09-07)

**动机**:`moe-progress-ratio-v12-0906` 证明了路由接口能区分「物体跟上来了」和「物体留在原地」(action token 上 L15 的 AUC 0.84,对照 coupled-vs-coupled 零假设 0.500)。这是一个**读数**结论。它不说模型使用了这份证据,更不说是哪条计算把证据变成了动作。本轮把问题换成计算问题:**新的交互反馈,究竟通过哪条计算改变 action chunk;物体丢失之后,这条计算在哪一步失效。**

**一句话总账**:**设计冻结、对照数据集造完(240 units / 960 rows,approach 与 transport 各 120),策略阶段卡在显存。实现过程中修掉四个错误,其中两个曾把 post anchor 的产出清零,一个是从别的研究抄来的阈值在裁量化噪声,一个是我抄常数而没有从协议导入。** 语料本身无法提供参考行为,这一点决定了整个设计。

产物在 `moe-coupling-circuit-0906/`,协议冻结在 `PROTOCOL.md`(渲染任何观测之前写的),每一处修订连同理由和测量值记在其 amendments 段。

---

## 1. 为什么要换问题:四个层次

| 层次 | 一个可能的实验结果 | 能支持什么 |
|---|---|---|
| 内部相关性 | 某个路由分数能区分抓住与没抓住 | 这个读数含相关信息 |
| 因果作用 | 改变某处内部计算,动作随之改变 | 该位置在所测条件下影响输出 |
| **计算机制** | **改变特定信息,经特定路径,引起可预测的动作修订** | **模型通过什么计算实现这类行为** |
| 机制指导的修复 | 依此修改模型,获得预期改善,并复测路径 | 解释具有指导改进的价值 |

项目此前的路由结果全部停在第一层。两个容易混淆的地方要写死:

- **能从激活里预测一个属性,不代表模型在使用它。** 外部探针可以利用网络中存在、但动作生成并未有效利用的信息。
- **干预后输出改变,也不等于知道该模块负责什么。** 破坏一个重要模块当然会让模型乱动;「它影响行为」到「它负责视觉纠偏」之间还差特异性、路径与对照。

所以要争取的不是「我发现一组与失败高度相关的专家」,而是:**我发现某类计算把某种交互证据转化成某种动作变化;针对这条计算进行修改,会产生事先预测的结果。**

---

## 2. 语料无法用筛选回答这个问题

「察觉物体丢了,修订动作」这个行为在 32,960 集语料里**一次都没有**。三个独立产物一致:

| 来源 | 数字 |
|---|---|
| `analysis_trap_taxonomy/results/belief_mismatch_summary.json` | `belief_mismatch_successes` = **0**,两个 16,000 集运行都是 0;约 229 个 phantom grasp 事件没有一个落在成功集里 |
| `himoe-route-capture/analyze_failure_behavior_taxonomy.py` | `active_retry` **0/307** 失败,`eef_oscillation` **0/307** |
| `analysis_moe_execution_signals/RECOVERY_HORIZON_PROTOCOL.md` | 短爆发恢复 **0/23** 事件状态 |

后果是硬的:**没有可追踪的参考计算,也无法从模型自己的输出里定义「正确的修订」。** LIBERO 的人类示范全部成功,里面同样没有滑落恢复,所以示范也补不上这个缺口。

参考计算必须构造。本轮的做法是把正控制放在**闭合之前**——一个模型确实会做的行为——把闭合之后的耦合丢失留作待解释的案例。

---

## 3. 轮次 1 的设计

### 3.1 用构造代替匹配

`object_readability` 的对照是筛出来的:phantom 事件按 task、手臂行程、query index 匹配 coupled 对照。它自己的产物里记着一条无法消除的混淆——**每一个 phantom 都落在失败集里,而 coupled 池 96% 成功**。

本轮从**同一个恢复的 MuJoCo 状态**生成全部条件。机器人构型、场景、相机、指令逐位相同,只有目标物体的 free joint 被改写。混淆不是被匹配掉的,是不存在。

`sim_states_before` 是每个 replan 步的 float64 完整状态,`validate_belief_mismatch.py:138` 早就在从里面按 `sim[q, lo:lo+3]` 读目标物体的切片;写这个切片和读它是对称操作。qvel 偏移由 `sim_layout.json` 的关节顺序推导(free 7→6,ball 4→3,hinge/slide 1→1)并与 `nv` 对账,不对就硬报错——错位会清零错误物体的速度。

### 3.2 两个锚点

都由夹爪开度与目标位置定义,不碰任何策略内部量。

- **`q_pre`**:夹爪仍张开、末端距目标 0.10 m 以内的最后一个 query。接近段。
- **`q_post`**:闭合之后目标抬起 ≥ 0.02 m **且**末端已离开闭合位姿 ≥ 0.10 m 的第一个 query。物体正在被搬走。

`q_post` 的离开条件用的是 taxonomy 判定 phantom grasp 的同一个阈值,所以 `uncoupled` 条件是那一类事件的**构造实例**,而不是另一个东西。

### 3.3 条件

| id | 锚点 | 编辑 | 角色 |
|---|---|---|---|
| `held` | 两者 | 无 | 参照 |
| `shift2/4/8` | `q_pre` | 目标在桌面内、垂直于「末端→目标」方位横移 2/4/8 cm,四元数不动,qvel 清零 | 分级正控制 |
| `uncoupled` | `q_post` | 目标写回 query 0 的静止位姿,qvel 清零 | 待解释案例 |
| `light` | 两者 | 只改光照与贴图,物体不动 | 视觉变了但动作不该变 |

**对你原方案的一处实质改动**:正控制从二值(在手 / 滑落)换成**闭合前的分级横移**。三个理由:分级剂量让闭合后的零结果**可解释**——结论变成「4 cm 位移引起的响应是 X,完全耦合丢失引起的响应不到 X/10」,而不是「没有显著效应」;横移给出连续有符号读数而非近乎二值的夹爪位;两个 regime 共用一族行为变量,前后对比才是同一把尺子。

方向取**垂直**于方位角而非沿着它:沿方位角移动改变的是距离,几乎不改变方位,而主读数是方位读数。

**需要写清的前提**:我没有验证过模型会在接近途中重新瞄准。语料能看到的是它能从不同初始物体位置成功抓取(Spatial 478/500,replan 5),这与会重新瞄准一致但不构成证据。**正控制本身就是第一个可证伪步骤。**

### 3.4 行为读数

chunk 是 (10, 7),LIBERO 的一行是 `[dx,dy,dz,drx,dry,drz,gripper]`。三个分量始终一起报:

- `B_dir` — 累计指令平移与「末端→目标**当前**位置」的水平余弦。**`q_pre` 主读数。**水平,因为受测位移是水平的;带上竖直分量会让每个条件共有的下降主导一个本该测方位的余弦。
- `B_grip` — chunk 内夹爪指令均值,加首次重新张开的 chunk 数。**`q_post` 主读数。**
- `B_mag` — 累计指令平移的范数。

`B_dir` 对 `B_mag` 才能区分「调整到了预期方向」与「只是变大、变随机」。`‖A_held − A_edited‖` 只作诊断量,绝不作效应量。

注册的**次要**读数 `B_track`:指令平移**差值**(edited 减 held,水平分量)与实际位移方向的余弦。零假设恰好是 0,且不继承接近本来就瞄在哪。它是次要的因为 `B_dir` 注册在先;无论两者表现如何都不事后提升。

**闭合后没有正确修订的参照,也不伪造一个。** `q_pre` 拿有符号、有方向验证的读数;`q_post` 拿变化的幅度与符号,对照「无变化」零假设,并在报告里这么写。

### 3.5 冻结的有效性门禁

复原容差、无穿模、有支撑、渲染非常数,以及最关键的一条——**可见性**:编辑必须在观测里出现,至少一个相机有 ≥1% 像素变化 ≥ 8/255。

没有这道门禁,闭合后的零结果无法解释:「证据从未进入观测」和「证据进入了但没被使用」是两个不同断言,而整个研究就是为了分开它们。`light` 也必须过同一道门——不改变任何像素的光照对照不是对照。

### 3.6 分阶段闸门

每一阶段都可以终止研究。终止的那一阶段产出书面负结果,不是放松阈值。

| 阶段 | 内容 | 闸门 |
|---|---|---|
| 1 | `q_pre` 上 `shift8` 是否让指令朝位移后的物体转 | 区间不含零且方向正确,`light` 不动;否则前提不成立,3/4 不跑 |
| 2 | `q_post` 上 `uncoupled` 在同一把尺子上的响应 | 报比值,不对零假设做显著性检验 |
| 3 | 只在 `q_pre` 上做模块级激活替换 | 某个模块要恢复 ≥25% 的行为差距,而范数匹配的随机方向 <10%;否则工具灵敏度不足以支持机制断言 |
| 4 | 在过闸门的模块上拆门控与内容 | 见下 |
| 5 | 同一批 arm 搬到 `q_post`,定位失效发生在哪一步 | — |

阶段 3 的替换位点包含**state token 与 action token 分开**——这是去检验而不是假设 `V → state token → action token` 这条路径。

### 3.7 事先注册的预测

阶段 4 的 arm(受体背景 = `shift8`,供体 = `held`):`mask_only`、`weight_only`、`route_full`、`content_only`、`shared_only`、`route_and_content`,加上 `magnitude` / `direction`。

| 假设 | 注册的预测 |
|---|---|
| 门控调度行为 | `route_full` 恢复 ≥50% 差距;`content_only` <20%;`mask_only` 承担 `route_full` 的大部分 |
| 专家变换输入 | `content_only` ≥50%;`route_full` <20% |
| routed 分支是增益 | `magnitude` 恢复得比 `direction` 多,且效应落在 `B_mag`/`B_grip` 而非 `B_dir` |
| 证据旁路了路由 | `shared_only` 至少追平 routed 各 arm,或没有 MoE arm 过阶段 3 而注意力输出替换过了 |
| 没找到特定机制 | 每个 arm 都只产生无方向扰动:`B_mag` 动,`B_dir` 不动 |

**幅度上限是已知的**:routed 分支占 MoE 块输出的 12–37%,但只占**残差流的 8–11%**;shared 占 67–93% 范数(旧报告的「5.6%」用的是第三个分母,`himoe-vla-moe-routing-decomposition-2026-08-15.md:203` 对过账)。这大概也是此前冻结 state 路由改变了**哪些**轨迹成功、却没改变均值的原因。

---

## 4. Stage 0 结果

主任务 `libero_object/pick_up_the_chocolate_pudding_and_place_it_in_the_basket`,capture `right-50x8-20260903`,400/400 成功。复制任务留给 `libero_spatial/pick_up_the_black_bowl_between_the_plate_and_the_ramekin_and_place_it_on_the_plate`(同样 400/400),在主任务分析完之后跑。

**240 units / 960 rows:approach 120,transport 120。**

丢弃 210 个 anchor,**全部在 approach**:

| 原因 | 数量 |
|---|---|
| 没有一个 shift 方向能同时过接触与支撑 | 138 |
| 目标方位角未定义(末端几乎在物体正上方) | 72 |
| transport anchor | 0 |

可见性中位数(相对 `held` 变化的像素比例):

| 条件 | 腕部相机 | agentview |
|---|---|---|
| `shift8` | 0.098 | 0.002 |
| `uncoupled` | 0.105 | 0.006 |
| `light` | 0.011 | **0.392** |

物体编辑落在腕部相机,光照对照是个 39% 的 agentview 大变化。这是对照该有的样子。

渲染 CPU-only(osmesa),约 13 分钟。130 MB 数据集按项目惯例 gitignore,`counterfactuals_object_pudding_audit.json` 带 sha256 与每一行的来源、门禁值、丢弃理由。44 个测试通过,ruff 干净。

---

## 5. 实现中修掉的四件事

这一节是本轮的主要增量,四个都在任何策略前向之前暴露。

**5.1 `q_post` 定得太早,post anchor 产出为 0。** 原定义是闭合后目标抬起 ≥2 cm 的第一个 query。那时闭合的夹爪还在桌面上方 2 cm,把物体写回静止位姿等于塞进指间,全部触发穿模门禁。加上「末端已离开 0.10 m」之后解决,并且顺带让 `uncoupled` 对齐了 taxonomy 的 phantom 定义。

**5.2 物理门禁误伤 `light`,post anchor 第二次归零。** `light` 渲染的是未编辑状态,闭合后自然继承夹爪与被携带物体的接触。门禁按「非 held 即视为编辑了物体」来判,于是每个 post anchor 都被判成「编辑后的物体碰到机器人」。改成按「是否真的移动了物体」分流。这条写了带假场景的回归测试。

同一处还有一个方向写反的逻辑:原来按穿透深度分级,`depth > -5e-3` 判失败——等于**放过深穿模、拦下浅接触**。改成对移动了物体的条件任何机器人接触即失败。MuJoCo 只在碰撞裕度内报接触,「无接触」本身就意味着已经离开机器人。

**5.3 夹爪容差 1e-4 在裁量化噪声。** 这个常数是从 `render_rich_event_inputs.py` 抄来的,而那项研究用的是 float64 sim state。量了 60 集:

| 位置 | 中位数 | p90 | 最大 |
|---|---|---|---|
| `q_post`(夹爪闭合静止) | 1.13e-6 | 1.90e-6 | 2.33e-6 |
| `q_pre`(手指张开运动) | **1.14e-4** | 2.02e-4 | 3.41e-4 |

1e-4 正好骑在 approach 处的分布中位数上,否掉 49/208 个 approach anchor,理由是 0.1 mm 的手指位置差异——比研究操纵的 20–80 mm 位移低三个数量级,也只有已接受的位置容差的一半。改为 1e-3。**这是唯一一处看到门禁在卡之后才改的阈值**,协议里连测量值一起记下,并且写明它只涉及输入保真度:当时没有跑过任何策略,不存在任何结果。

**5.4 flow noise 形状写成 (10, 32),应为 (10, 24)。** 内部 action dim 是 24(`server_metadata.json` 里就写着)。我在渲染脚本里硬编码了常数而没有从 `himoe_libero_bridge.protocol` 导入。宽度错不只是形状错——RNG 消耗方式也不同,所以噪声连 capture 的抽样都复现不了。现在从协议导入,并在加载 7.6 GB 权重**之前**先校验契约:第一次是深入到第一次前向内部才炸的。

还有一处产出统计的偏斜:approach 与 transport 共用一个 unit 预算,而 transport 的通过率接近 100%,于是预算被 transport 填满、把作为阶段 1 闸门的 approach 饿死(240 units 里 208 个 post)。改成按锚点分别计预算。

---

## 6. 阻塞

8 张 H20-3e 每张 139430/143771 MiB 被**容器外**的 8 个进程占着,剩约 4.3 GB;策略需要约 16.3 GB(`server_metadata.json` 的 `gpu_memory_allocated_bytes`)。

CPU 兜底不成立:这个 checkpoint 的 AS gate 把输入转 bfloat16 而权重留 float32,只在 CUDA autocast 下成立(`modeling_moe.py:132`)。绕过它要改冻结的 upstream 计算,不做。`--allow-cpu` 仍保留,它能加载 checkpoint 并验证观测与 flow-noise 契约——5.4 那个错误就是这样抓到的——但拿不到 action。

**腾出一张约 17 GB 的卡即可跑阶段 1/2。**

---

## 7. 阶段 3/4 的落点与一个采集缺口

补丁位点已经摸清:

- `himoe_state_recorder.discover_blocks(core)` 给出 8 个 HBMoE 块,带 `module.gate`、`module.experts[i]`、`module.shared_experts`、`n_experts`、`top_k`。
- `moe_infer(x, flat_expert_indices, flat_expert_weights)` 是 dispatch 入口;`himoe_functional_recorder.py` 已经用替换这个方法的方式做过一次完整捕获,阶段 4 的六个 arm 都能在同一处加上 `shared_experts` 的 forward hook 实现。
- 供体与受体是**同一个 MuJoCo 状态**的两次前向,比 `rollout_patch_probe.py` 的跨 seed 供体匹配得好得多,也不再需要「供体必须来自同进程活体 rollout」那个约束(相同 seed 重放曾给出 27/64 对原始 23/64)。

**缺口**:`content_only` 需要在供体输入上评估受体选中、而供体没有选中的专家。现有 functional recorder 明确「不重复评估专家」,且只存 32 维 sketch 不存完整向量。这条 arm 要新写采集路径:少量冻结状态上保留完整的 `F_e(h)` 与 `s(h)`。

阶段 3/4 的代码**故意没写**:协议把阶段 1 设成硬闸门,如果模型对 8 cm 位移不产生方向性响应,这个行为里就没有可定位的「证据→动作」计算,写替换扫描是浪费。

---

## 8. 这份工作不主张什么

- Stage 0 **没有**建立任何关于动作响应的结论,更没有任何关于内部计算的结论。
- 编辑后的状态是经过物理门禁的**复原**,不是仿真器一步步走到的状态:接触历史与沉降都不存在。
- RGB 由存储的 MuJoCo 状态重新渲染,不是回放存储的帧。
- 轮次 1 是单任务加一个复制任务。这里没有任何东西支持跨任务的一般性陈述。
- 冗余在这里不可解:某模块消融无效可能是它不重要,也可能是别的路径补偿了;多模块同时删除效应大也可能只是总损伤大。替换后的激活偏离自然分布,每个效应都相对于所选背景与供体而言。
