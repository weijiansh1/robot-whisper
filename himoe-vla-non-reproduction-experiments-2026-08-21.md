# HiMoE-VLA 近期非复现实验报告

> 范围：2026-08-07 至 2026-08-21 的探索、消融与机制实验；不包含论文指标复现、checkpoint 对齐和环境排障。

## 1. HB-MoE 计算与路由选择的因果消融

**实验思路：** 在相同初始状态和 flow noise 下，分别随机替换 HB 的 Top-4 专家、关闭 routed 分支、关闭 shared 分支或关闭整个 HB-MoE 子层，用配对成功率区分“MoE 计算是否重要”和“router 选谁是否重要”。

**结果：** 在 `released-left` 动态范围点，基线为 14/50（28%）；随机 Top-4 为 16/50（32%，McNemar p=0.83），整个 HB-MoE 子层关闭为 5/50（10%，p=0.049）。另一轮四臂配对中，基线、shared-off、routed-off、block-off 分别为 28%、30%、24%、14%；单独关闭任一分支均无显著影响，两支同时关闭才稳定呈下降方向。结论是 HB-MoE 内部计算有用，但 shared/routed 两支存在较强功能冗余，离散专家选择本身不是稳定的行为控制量；50 局规模不足以精确估计 block-off 的效应量。

## 2. Router 三层机制拆解

**实验思路：** 将路由拆成“专家身份、Top-4 权重、专家函数”三层，检查 Top-K 边界稳定性，并把选中专家替换为第 5-8 名或随机专家，测量 MoE 输出和最终动作变化。

**结果：** action-token router 接近均匀：Top-4 总概率约 0.139-0.158（均匀值 0.125）；23.8% 的位点第 4/5 名分数完全相等，model Top-K 与确定性排序约 13.4% 不一致。专家输出彼此近正交，并非同一函数；将四个专家全换成第 5-8 名，Goal 上 MoE 输出/动作 relative L2 改变 29.7%/5.34%，随机替换为 31.3%/5.98%；Spatial 上分别为 48.1%/18.3% 和 50.1%/19.2%。因此专家函数不可互换，但 router 的微小分数差不能反映专家的反事实可替代性。

## 3. State-token 路由是否是机器人位姿编码

**实验思路：** 在多任务、多 checkpoint 上双向解码“本体状态 -> 专家选择”和“路由 -> 本体状态”，再到同时含末端位姿与关节角的 CALVIN-D 上判断路由更贴近哪种控制坐标。

**结果：** LIBERO 五个任务中，位姿预测专家选择的每层中位 `|AUC-0.5|` 为 0.304-0.480；路由反向恢复多数位姿维度的 LOSO R² 为 0.78-0.98，非线性本体模型可解释 86.3%-94.7% 的路由方差。CALVIN-D 的 3,621 个控制步上，路由恢复关节角的平均 R² 为 0.831，高于末端 7 维的 0.584；关节角单独可解释 57.2% 的路由方差，末端为 45.7%。state-token 路由主要是随动作空间变化的机器人本体/位姿码，而不是独立的任务价值状态。

## 4. 固定路由的因果干预

**实验思路：** 对同一任务的 512 组配对初态和噪声，分别把 state-token 路由固定为“成功原型”和“失败原型”，比较成功率以及具体成功样本是否被重排。

**结果：** 基线、pin-off、pin-on 成功率分别为 500/512（97.66%）、493/512（96.29%）和 504/512（98.44%）；相对基线差为 -1.37 pp（95% CI [-3.32,+0.59]）和 +0.78 pp（[-0.78,+2.34]），均不显著。干预却改变了 87.3%-89.5% 的轨迹，且基线结局与干预后结局的 phi 由 1 降至 0.106/0.085。路由干预会大幅重排“哪些局成功”，但没有稳定改变总体成功率。

## 5. Best-of-K、动作云选择器与自适应预算

**实验思路：** 在同一状态生成多个候选动作，先量化 oracle 最优候选随 K 增长的机会，再测试 medoid、anti-medoid、commitment、PCM 和低容量监督动作选择器能否兑现该机会；同时测试 state routing 是否能预测候选分散度并用于自适应 K。

**结果：** 在 14 个可用 fork 快照上，oracle 机会从 `O4=0.0121` 增至 `O32=0.0216`，但 medoid 的 `G8=-0.0018`、`G32=-0.0032`，anti-medoid、commitment、PCM 也均未稳定获得正收益；action-only LOSO 排序相关为 -0.034。state routing 预测动作分散度的 R²=-0.055、Spearman=0.240，弱于原始 proprio 的 R²=0.067、Spearman=0.342。候选池中确实存在更好动作，但所测的状态无关动作云几何和路由预算指标无法跨状态找出它们。

## 6. 初始条件与结局承诺

**实验思路：** 对每个任务采集 16 个初始场景、每场景 32 个独立 flow-noise rollout，在控制步 0 计算仅由初始场景决定的结局熵，并比较路由、物理状态及场景组合的结局读出能力。

**结果：** Long t08 在模型尚未动作时，初始场景已解释 44.3% 的结局熵，16 个场景中 3 个结局完全锁死；其他非满分任务解释比例为 18.7%-56.6%。t08 上只用场景的交叉验证解释率为 38.2%；控制步 0 的路由为 36.8%、物理状态为 33.3%、路由+场景为 36.7%。早期路由没有提供超出初始条件/物理状态的独立承诺信号。

## 7. 路由失败规则及其增量信息

**实验思路：** 搜索状态 token 上两条 `(层, 专家)` 条件的合取规则，以场景内置换控制多重比较；再用跨场景划分、位姿孪生、完整仿真状态残差化和加入动作块的诚实划分检验规则是否可迁移、是否只是位姿或动作意图的代理。

**结果：** 不存在可靠的“某路由组合必失败”硬规则：最大全失败支持为 30，置换零假设 95% 上限为 29。存在可迁移软规则 `L2:e5 & L5:e8`，触发时失败率为 69%，场景基线为 49%；t08 跨场景前 20 规则的留出 z=2.22+/-0.31，打乱对照为 0.19+/-0.19。位姿孪生后前 20 规则并集仍有 +11.9 pp 的失败差；诚实划分并扣除随机规则后，在 t08 上相对完整物理状态增加 +0.0129+/-0.0035，加入动作块后仍为 +0.0108+/-0.0034。该增量只在最难的 t08 上成立，其余三个任务接近 0。

## 8. 失败轨迹的动力学停滞

**实验思路：** 将 state-token 路由视为机器人位姿的离散量化，同时构造相同分辨率的原始位姿分区，比较成功/失败轨迹的发散、重访、停留、速度和逃逸率，判断是否存在路由特有的失败吸引子。

**结果：** t08 的同场景异结局轨迹开始显著分离：位姿在第 8 步、路由在第 13 步，路由没有领先物理状态。失败轨迹比成功轨迹访问更少格子、切换更少、重访更多、停留更久；但相同现象在位姿分区中同样或更强。失败局平均位姿速度为 0.853，成功局为 1.006（场景校正 z=-10.38）；连续最慢三步为 0.168 对 0.487。停留至少 7 步的路由格中速度仅 0.207（全局 0.942），92.2% 来自失败局。结果支持“策略进入物理慢速停滞/局部循环”，不支持独立的路由吸引子解释。

## 9. 早期路由减枝与动作投票

**实验思路：** 在 Goal 的 10 个任务、50 个 case、701 个 replan pool 上，从 K=8 候选的前 r 个 flow step 计算路由距离，保留路由中心的 M 个候选，再用完整动作块 medoid 投票；与精确随机子集和初始噪声中心性比较。

**结果：** 路由前缀能预测最终动作相似性，但始终弱于初始噪声：第 1 步路由 pair AUC=0.607，初始噪声 AUC=0.660；即使使用完整 10 步路由，AUC 也只有 0.639。第 1 步保留 M=4 时，理想 flow 算力节省 45%，保留完整 K medoid winner 的比例为 65.3%，高于随机子集的 50.0%，但低于初始噪声中心性的 72.2%；M=6 时分别为 22.5%、84.9%、75.0% 和 90.0%。因此当前结果不支持上线 routing-based early pruning：它是离线影子结果，目标只是动作共识，而且被无需运行 router 的初始噪声基线支配。
