# HiMoE-VLA MoE 路由状态分析

日期：2026-08-07
模型：HiMoE-VLA-Libero-Goal，`checkpoint_sha256 = 98ee29d09d1855716e341532a3d0f76068de6fa131f8fe16ffd52f982df1b953`
代码：`himoe-libero-wrist-fix` @ `d779741`，上游 HiMoE-VLA `src/moevla/models/`
硬件：单张 H20-3e 的 MIG `1g.35gb` 分片（12 SM / 32.5 GiB）
产物：`/home/jovyan/work/himoe-route-capture/`

---

## 一句话结论

**MoE 的计算重要，MoE 的路由决策不重要。** 在有动态范围的操作点上（`released-left`，基线 28%），
把 8 个 HB 层的整个 MoE 子层置零，成功率掉到 10%（McNemar 配对 p = 0.049）；
但把 router 的 top-4 选择换成**均匀随机**，成功率是 32%，与基线无差异（p = 0.83）。
换句话说：那 8 个 MLP 子层在做有用的事，**但用哪 4 个专家去做无关紧要**。

AS-MoE 在 LIBERO 上则是一个输出恒定的常量路由器，其中 layer 1 的专家选择由 bf16 舍入决定。
HB router 的 softmax 熵是 ln(32) 的 98.4–99.8%，有效专家数 30.3–31.8（满值 32）。

路由状态确实携带信息（100% 解码 wrist 配置，扣除场景后 0.710 预测成败），
但那是**读出**（readout），不是**控制**（control）。

共跑 438 个 LIBERO episode。

> **对早期结论的更正。** 本报告先在 `checkpoint-right` 上做消融，那里基线是 50/50 满分——
> 天花板是检测退化最不敏感的工作点，四种消融全部得到 25/25，Wilson CI 仅能排除 >13.3 点的掉幅。
> 在 `released-left` 上重跑后，block 消融的效应显现出来（28% → 10%），
> 而 router 消融的零效应得到确认。**只有后一组数字应被引用。**

---

## 一、实验清单

| 批次 | 配置 | 集数 | 用途 |
|---|---|---:|---|
| 探路 | task 0，两种 wrist layout | 8 + 8 | 建链路 |
| 主对比 | task 0 全部 50 个官方初始状态 | 50 + 50 | layout 对比 |
| 组内 | 8 个 init state × 10 个 flow noise | 80 | 扣除场景难度 |
| recorder 验证 | checkpoint-right | 6 | 双实现对账 |
| 完整概率 | `--store-full-probs` | 25 | router 锐度 |
| 反事实探针 | 离线替换路由 | 3 | block 层分解 |
| 消融（天花板） | right + {random, worst, shared_off, block_off} | 25×4 | 灵敏度不足，见 5.1 |
| **消融（敏感点）** | **left + router random** | **50** | **结论所在** |
| **消融（敏感点）** | **left + block_off** | **50** | **结论所在** |
| 四层状态捕获 | router/block/expert 全档 | 4 | 工具验证 |
| 其它 | smoke / recorder 对账 / 客户端验证 | 10 | — |

协议全部沿用已审计的设置：环境 seed 7、settle 10 步、replan 10、上限 300 步、
每集固定 flow noise。

---

## 二、AS-MoE：在单具身基准上是常量

AS gate 的唯一输入是 `data_mask`，而 LIBERO 的 `data_mask` 是常量
`LIBERO_DATA_MASK = [1]×7 + [0]×17`（`data_contract.py:23`）。wrist layout 只改图像槽位和
图像 mask（`policies.py:186-199`），不碰 `data_mask`。因此两种 layout 的 AS 路由**按构造相同**，
且在整个 episode、所有 denoising step、所有 suffix token 上恒定。

直接从 checkpoint 权重计算，并在 312 个真实 control step 上验证一致：

| AS 层 | 选中专家 | 该专家概率 | 三专家完整分布（fp32） |
|---|---|---|---|
| 0 | 2 | 0.562 | [0.206, 0.232, 0.562] |
| 1 | **0（bf16）/ 2（fp32）** | 0.333 | [0.33325, 0.33325, 0.33334] |
| 16 | 0 | 0.394 | [0.394, 0.334, 0.272] |
| 17 | 1 | 0.466 | [0.235, 0.466, 0.298] |

**layer 1 是一个退化路由器。** 三行权重在被 `data_mask` 激活的前 7 维上的和是
`[0.250732, 0.250732, 0.250854]`，最大两行只差 `1.22e-4`。模型实际以 bf16 运行
（`policy.py:71` 的 `torch.autocast`），三个 logit 精确塌成 0.25，`torch.topk(sorted=False)`
按索引返回 0；同样的权重在 fp32 下会选专家 2。**这一层选哪个专家由浮点舍入决定，不是学出来的。**

用不同宽度的 mask 探测确认 AS router 本身有区分度（前 6/8/10/14/20/24 维为 1 时 top-1 会变），
所以它不是死的——只是 LIBERO 永远只有 7 维，它在 Section 4.1 的全部实验里贡献零自适应性。

---

## 三、HB-MoE：路由几乎是均匀的

312 个 control step × 8 层 × 10 denoising step × 11 token = 274,560 个路由站点的完整 32 维 softmax。

| 层 | 熵 | 熵 / ln32 | top-1 概率 | top1/均匀 | top1−top2 | top-4 质量 |
|---|---|---|---|---|---|---|
| 2 | 3.4364 | 0.9915 | 0.0476 | 1.52× | 0.00828 | 0.159 |
| 3 | 3.4383 | 0.9921 | 0.0473 | 1.51× | 0.00855 | 0.158 |
| 4 | 3.4110 | 0.9842 | 0.0601 | 1.92× | 0.02371 | 0.166 |
| 5 | 3.4456 | 0.9942 | 0.0445 | 1.42× | 0.00658 | 0.155 |
| 12 | 3.4592 | 0.9981 | 0.0379 | 1.21× | 0.00155 | 0.145 |
| 13 | 3.4590 | 0.9981 | 0.0378 | 1.21× | 0.00145 | 0.145 |
| 14 | 3.4598 | 0.9983 | 0.0375 | 1.20× | 0.00149 | 0.143 |
| 15 | 3.4593 | 0.9981 | 0.0378 | 1.21× | 0.00167 | 0.144 |

参考：均匀分布 H = ln(32) = 3.4657，top-1 = 0.03125，top-4 质量 = 0.125。

**有效专家数 exp(H) = 30.3 ~ 31.8（满值 32）。** 被选中的 4 个专家总共只占 14.3–16.6% 的概率质量，
而随便挑 4 个是 12.5%。晚层（12-15）比早层更接近均匀。

这**不是**"负载均衡良好"。负载均衡说的是聚合统计量均匀，与每个 token 上的分布是否尖锐无关——
一个高度专业化的 router 完全可以既 per-token 尖锐又聚合均衡。这里塌掉的是 per-token 分布本身。

router 并非对输入完全无反应：把 log p 拆成"每个专家的常数偏好"+"随输入变化的残差"，
**85–97% 的方差是输入驱动的**。但绝对幅度极小（log-prob 方差 0.012–0.055），
所以 top-4 集合churn 得很厉害：34,320 个站点上出现约 10,000 个不同集合
（C(32,4)=35,960），相邻控制步的集合 Jaccard 只有 0.16–0.22。

---

## 四、专家彼此正交，但 shared 分支主导输出

**专家权重差异**（checkpoint 直接分析，每层 32 个专家 × 3 个投影矩阵）：

- 两两余弦 ≈ **0.0000**，全部落在 ±0.004
- `‖W_e − W_mean‖ / ‖W_mean‖` = **5.53 ~ 5.57**，而 32 个独立随机向量的理论值是 √32 = 5.66

所以专家不是彼此的复制品，每个都在算真正不同的函数。（注意这个指标区分不了
"训练出了分工"和"接近随机初始化"——两者都给出 ≈0 的余弦。）

**反事实路由**（24 个真实 control step × 10 denoise × 8 层 = 1920 次测量）：

| 层 | routed 分支：随机 top-4 vs 真实 | routed 占 block 输出 | **block 输出：随机 vs 真实** |
|---|---|---|---|
| 2 | 1.090 | 0.327 | 0.440 |
| 3 | 1.096 | 0.357 | 0.490 |
| 4 | 1.102 | 0.383 | 0.532 |
| 5 | 1.121 | 0.391 | 0.558 |
| 12 | 1.171 | 0.230 | 0.326 |
| 13 | 1.193 | 0.185 | 0.266 |
| 14 | 1.175 | 0.133 | 0.195 |
| 15 | 1.162 | 0.067 | **0.118** |

第二列 >1.0 说明换一组专家后 routed 分支的输出与原来几乎不相关——专家确实各干各的。
但第三列显示 **routed 分支只占 block 输出的 33%→7%，随层深单调下降**，
到 layer 15 时始终激活的 shared expert 以约 14:1 压倒 routed 分支。
所以在残差流真正看到的量级上，重新路由只让 layer 15 的 block 输出变化 11.8%。

### 四层完整状态捕获的独立复核

用 `himoe_state_recorder.py`（router / block / per-expert / raw 四档）在 36 个真实 control step 上
重测，与上面的离线探针**独立对账通过**：

| 层 | routed 占 block 输出 | 探针给出的值 | ‖out‖/‖in‖ | cos(in,out) | cos(routed,shared) | shared/routed |
|---|---|---|---|---|---|---|
| 2 | 0.326 | 0.327 | 0.301 | 0.353 | 0.206 | 2.1× |
| 5 | 0.391 | 0.391 | 0.213 | 0.247 | 0.191 | 1.6× |
| 12 | 0.231 | 0.230 | 0.284 | 0.415 | 0.230 | 3.3× |
| 15 | 0.068 | 0.067 | 0.909 | 0.471 | 0.291 | 13.7× |

`cos(routed, shared)` = 0.19–0.29，说明 routed 分支不是 shared 的缩放副本，确实在加不同的东西；
但它的量级随层深塌到 1/14。

**dispatch 是均衡的。** 每个专家平均收到 1.38 个 token，与完全均匀的 11×4/32 = 1.375 几乎相等，
变异系数 0.33–0.47。这正是 load-balancing aux loss 该做到的事——**聚合负载均衡与 per-token
分布尖锐是两回事，这里前者成立、后者塌了。**

**routed 分支主要作用在 state token 上，不在产生动作的 action token 上：**

| 层 | routed 占比 @ state token | routed 占比 @ action token |
|---|---|---|
| 2 | 0.546 | 0.257 |
| 5 | 0.536 | 0.338 |
| 12 | 0.405 | 0.204 |
| 15 | **0.381** | **0.056** |

`moevla.py:483` 取的是 `suffix_out[:, -n_action_steps:]`，即 action token 才产生动作，
state token 被丢弃。**在真正决定输出的那些 token 上，layer 15 的 routed 分支只贡献 5.6%。**

AS block 的 shared 范数精确为 0（`ASMoEConfig.n_shared_experts = None`），
AS per-expert token 均值 `[5.5, 2.75, 2.75]` 也与路由 `[2, 0, 0, 1]` 的预期严格一致
（专家 0 占 4 层中的 2 层 → 11×2/4 = 5.5）。

---

## 五、行为学消融：计算重要，路由不重要

前四节都是内部量级的分析。真正的判据是行为。用 forward hook **替换** 模块返回值，
让消融在真实前向里生效（不改上游源码），只动 8 个 HB 层，AS 层不动。

消融机制经单元测试验证：`shared` 置零后 block 输出精确等于 `y0 − shared`；
block 置零后范数精确为 0；移除 hook 后完全复原。

### 5.1 天花板上的结果（信息量低）

`checkpoint-right` 基线是 50/50 满分：

| 条件 | 成功 | 平均步数 |
|---|---|---|
| 未改动 | 25/25 | 120.8 |
| HB router → 均匀随机 top-4 | 25/25 | 119.6 |
| HB router → 概率最低的 4 个专家 | 25/25 | 118.4 |
| shared expert 置零 | 25/25 | 119.4 |
| 整个 HB-MoE 子层置零 | 25/25 | 119.8 |

四种消融全部生效（轨迹改变，20–22/25 的步数与基线不同），全部 25/25。
但 25/25 的 Wilson 95% CI 是 **[0.867, 1.000]**——只能排除 >13.3 点的掉幅。
**天花板任务无法区分"无影响"和"影响不到 13 个点"。这组数字不应作为结论引用。**

### 5.2 有动态范围的操作点（结论所在）

`released-left` 基线 14/50 = 28%，50 集，与基线共用 init state 与 flow noise 种子，配对检验：

| 条件 | 成功 | Wilson 95% CI | McNemar 配对 p |
|---|---|---|---|
| 未改动（基线） | 14/50 = 28% | [17.5%, 41.7%] | — |
| HB router → 均匀随机 top-4 | **16/50 = 32%** | [20.8%, 45.8%] | **0.83**（10↓ / 12↑） |
| 整个 HB-MoE 子层置零 | **5/50 = 10%** | [4.3%, 21.4%] | **0.049**（13↓ / 4↑） |

**router 消融的翻转是对称的（10 降 12 升），block 消融是单向的（13 降 4 升）。**

结论分成两句，不能合并：

- **那 8 个 HB MLP 子层在做有用的计算。** 整个置零掉 18 个点。
- **router 选哪 4 个专家无关紧要。** 均匀随机替换后成功率不降反升 4 个点（在噪声内）。

需要说清被移除与保留的范围：置零的是 18 层中 8 层的 MLP 子层。
**全部 18 层的 attention、6 层 dense MLP、4 层 AS-MoE、PaliGemma 前缀均原封不动。**

### 5.3 一个被证伪的预测

基于第四节的范数分解（shared 分支占 block 输出 67–93%、action token 上 94%），
本报告曾预测"去掉 shared 分支会大幅掉点"。**实测 25/25，不掉。**
范数占比不等于功能重要性——shared 分支贡献了绝大部分幅度，但那个幅度是冗余的。
（该预测只在天花板上检验过；在 `released-left` 上单独消融 shared 分支尚未运行。）

---

## 六、路由是有信息的——但那是读出，不是控制

同一批路由状态在两个判别任务上都远超随机：

| 任务 | 留一交叉验证平衡准确率 | 置换检验 p |
|---|---|---|
| 预测 wrist layout（right vs left） | **1.000**（每一层单独也是 1.000） | 0.0033 |
| 预测成功/失败，**扣除场景后** | **0.710** | 0.0060 |
| 同上，不扣场景（会混进场景难度） | 0.859 | — |
| 对照：仅用 robot state 预测成败 | 0.623 | 0.08（n.s.） |

"扣除场景"指的是：同一个 init state 重复 10 次、只换 flow noise，特征按 state 去均值
（去均值在每个 CV fold 内只用训练集拟合），置换检验在每个 state 内部打乱标签。
8 个 init state 中 6 个出现混合结果（成功率 0/10、7/10、0/10、2/10、9/10、7/10、4/10、8/10），
共 60 集可用。各场景的"成功减失败"方向平均余弦 0.297，组内置换零分布 0.001，p < 0.0001——
是一条跨场景共享的轴。

这两件事不矛盾。路由是 hidden state 的一个函数，hidden state 携带了输入配置和轨迹质量的信息，
所以路由能被解码出这些信息。但第五节证明改变路由不改变行为。**路由是模型内部状态的一面镜子，
不是方向盘。**

---

## 七、wrist layout 的影响（附带结果）

| 组 | 成功 | 平均步数 | 平均推理次数 |
|---|---|---|---|
| checkpoint-right（正确） | **50/50** | 120.8 | 12.5 |
| released-left（错误，左手槽位） | **14/50** | 269.5 | 27.1 |

HB 路由被 wrist 错配扰动的程度随层深增大。第一次推理的配对对比（同初始状态、同 settle、
同 flow noise，唯一差异是槽位）的 top-4 集合 Jaccard：

| HB 层 | 跨 layout（配对） | 组内跨 episode 基线 | 随机基线 |
|---|---|---|---|
| 2 | 0.648 | 0.235 | 0.074 |
| 5 | 0.539 | 0.250 | |
| 12 | 0.251 | 0.189 | |
| 15 | 0.187 | 0.176 | |

早层换槽位后仍保留 0.54–0.65 的重叠，**高于换一个初始状态造成的 0.24–0.32**；
晚层已掉到"完全不相干的另一条 episode"的水平。

路由漂移量（相邻控制步的平均 TV）在 100 集上**零重叠**：right 最低 0.703 > left 最高 0.670，
置换检验 p = 0.0001。但这个粗指标分不出成败（Mann-Whitney p = 0.20，按 state 配对 p = 0.69）。

---

## 八、方法与工具

### 双实现对账

自建 recorder（`himoe_router_recorder.py`）与仓库原有的 `_HBRouteCapture`
（`policies.py:300-360`，已随 500-episode 正式评测验证）在同一条 episode 上比对：

```
HB expert_ids  逐位比对 12×10×8×10×4 = 38,400 个路由位点
完全一致: True    一致率 1.000000
```

轨迹未被扰动：装载 recorder 后的 3 集与未装载的 right50 逐集一致（120/12、103/11、125/13）。
AS 折叠断言在 312 个真实 control step 上 **312/312** 成立。
从存储的 raw 概率重建 `combine_weight`，与实测 `topk_weight` 最大误差 2.71e-04。

捕获开销：真模型 **2.4–2.7 ms / control step = 0.28–0.33%**
（合成模型上测得的 16.9% 是分母太小造成的假象——那个假模型没有 attention 和 PaliGemma prefix）。

### 只有接真实路径才暴露的缺陷

`atexit` 里 flush Zarr 会报 `RuntimeError: cannot schedule new futures after shutdown`——
解释器退出时 Zarr 的 async executor 已拆，最后一个不满 chunk 的分片**静默丢失**，
且错误发生在退出路径上极易被忽略。改为在 SIGTERM/SIGINT 处理器里 flush（事件循环仍在）
再 `os._exit`。

### 存储

真实 trace 形状 `[flow=10, layer=8, action=10, topk=4]`，**9.38 KiB / control step**。
（注意 LIBERO checkpoint 的 `n_action_steps=10`，suffix 只有 11 个 token；
上游默认配置的 50 步对 CALVIN 成立，对 LIBERO 不成立。）

3,943 个真实 control step 上实测：

| 配置 | 压缩比 | B/control step |
|---|---|---|
| npz_compressed | 2.20× | 4,363 |
| zarr chunk=256 zstd-3 | 2.48× | 3,869 |
| zarr chunk=256 zstd-19 | 2.73× | 3,513 |
| + 权重量化 uint8 + 只存前 3 个 | **4.01×** | **2,394** |

两个技巧：top-4 权重归一化后和为 1，**第 4 个可由前 3 个推出**，白省 25%；
量化成 uint8 后重建 `combine_weight` 的最大绝对误差 3.9e-03。

**bitshuffle 有害。** fp16 权重数组裸 zstd-5 压 2.95×，加 bitshuffle 只有 1.85×（反而大 60%）。
真实 combine weight 归一化后密集聚在 1/k 附近，字节层面高度重复，被 bitshuffle 打散了。
早期基于合成数据的相反建议是错的，默认值已改回裸 Zstd。

---

## 九、限制

1. **单任务。** 全部 438 集都是 LIBERO-Goal task 0（"open the middle drawer of the cabinet"）。
   消融结论不应外推到其它任务、其它 suite 或 CALVIN。
   0. **消融结论的统计强度有限。** block 消融的 McNemar p = 0.049，Fisher p = 0.040，
   都只是勉强越过 0.05；50 集的 Wilson CI 仍然很宽（10% 的 CI 是 [4.3%, 21.4%]）。
   router 消融的零效应结论同理：p = 0.83 说明"看不出差别"，不等于"证明了没有差别"，
   CI [20.8%, 45.8%] 与基线 [17.5%, 41.7%] 大幅重叠。要把任一结论收紧需要 150-200 集。
2. **单 checkpoint。** 只测了 Goal。Spatial / Object 的 router 可能不同。
3. **消融只针对 HB。** AS 层未消融（它本来就是常量，消融它等价于换一个固定专家）。
4. **专家权重的余弦 ≈ 0 区分不了"训练出分工"和"接近随机初始化"。** 要判定需要初始权重
   或训练轨迹，两者都未公开。
5. **第六节的因果方向未定。** 路由能预测成败，但路由、动作、结果都由同一份 flow noise 驱动；
   路由是中介还是伴随现象，本实验不能区分。
6. 晚层去相关的结论只有第一次推理是严格配对的；后续控制步轨迹已分岔。

---

## 十、可以直接接着做的

- **把消融扩到 Spatial / Object / 其它 task。** 每个条件 25 集约 7 分钟，成本很低。
  如果随机路由在所有 suite 上都不掉点，这就是关于该发布权重的一个强结论。
- **消融 shared expert。** 既然它占 block 输出的 67–93%，把它去掉应该会显著掉点——
  这能确认 MoE block 的作用几乎全部来自那个始终激活的分支。
- **同一初始状态下换 flow noise 的成败分化**已经建好（`--init-state-ids` + `--repeats`），
  可以扩到更多场景提高第六节的功效。
- **在 `released-left` 上单独消融 shared 分支和 routed 分支**：现在只知道整体置零掉 18 点、
  路由选择无关，还不知道这 18 点由哪一支承担。天花板上 shared_off 不掉点，
  但那个操作点没有分辨力。
- **提高样本量到 150-200 集**收紧 p 值——目前两个关键结论都在 0.05 边缘或依赖零效应。
- 四层捕获（`himoe_state_recorder.py`）的 `raw` 档尚未在真实 rollout 上跑过，
  用于回答"每个专家收到的具体是哪些 token"。

---

## 附：产物清单

`/home/jovyan/work/himoe-route-capture/`

| 文件 | 说明 |
|---|---|
| `himoe_router_recorder.py` | router 层捕获，AS+HB，raw 概率 / entropy / 可选完整分布 |
| `himoe_state_recorder.py` | 四层完整 MoE 状态捕获（router/block/expert/raw 分档） |
| `himoe_route_store.py` | Zarr v3 + Zstd 写读，按 dtype 配 codec |
| `serve_with_recorder.py` | 挂 recorder 的模型服务器（不改上游源码） |
| `serve_ablated_router.py` | 路由消融服务器（none/random/worst/uniform） |
| `serve_state.py` | 挂四层 recorder 的服务器 |
| `probe_counterfactual.py` | 离线反事实路由探针 |
| `rollout_with_routes.py` | LIBERO rollout 驱动，支持 init-state × noise 重复 |
| `analyze_*.py` | 各项分析与对照检验 |
| `make_figures.py`, `fig_*.py` | 图 1-8 |
| `runs/` | 288 集完整 trace（34 MB） |
| `figures/` | 全部图 |
